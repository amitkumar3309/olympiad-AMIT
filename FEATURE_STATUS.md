# FEATURE_STATUS.md

Status values: `NOT_STARTED`, `IN_PROGRESS`, `IMPLEMENTED` (works end-to-end, verified by reading the actual data path — not just "a page exists"), `TESTED` (has automated test coverage), `DEPLOYED` (live in production).

A UI page existing with hardcoded/mock data is recorded as its own row/note — it does **not** count as `IMPLEMENTED` for the underlying feature.

**Infrastructure rows** (below the feature table) track the Milestone 1 backend foundation separately, since those are cross-cutting capabilities rather than user-facing features.

_Last updated: 2026-08-29, after **Milestone 23, Phase H (the final regression)** — which closes the milestone. Every flow was driven end to end in a browser against a real backend and a real database: a new student **registered through the form** and got `AMIT_1702`, then ran a **practice session** to a server-marked review, answered the **daily challenge** (streak 0 → 1, +15 XP), sat a **mock test** under a server-owned countdown, and confirmed their **referral code** resolves publicly with the name masked; as the root super administrator, a question was **created and published**, the generated Excel template was **uploaded back through the importer** and dry-run validated, and a real **invoice PDF** and **student export** were downloaded. **139 API calls across 35 routes, zero non-2xx, zero JavaScript errors.** It found two defects, both in the answer runners: **a failed answer save left the counter claiming success** (a mock test read "3 answered", the dialog said "All 3 questions are answered", and the paper came back 0/12 with every question NOT ANSWERED — both runners now roll the answer back and say so), and **two heading skips that only appear once a page has data**. 31 obsolete CSS rules were removed. **No backend file was touched in this phase or in the seven before it** (1253 tests across 35 files, unchanged) and the endpoint surface is byte-identical to where the milestone started. Before that, **Milestone 23, Phase G (the responsive and accessibility audit)** — every important page measured in a browser at 375, 768, 1280 and 1440px, in both themes, signed out, as a student and as the root super administrator. It found two colour rules nobody had written down: **a fill colour is not a text colour** (109 declarations used `--success` / `--warning` / `--danger` as text — "Paid" measured 2.22:1, "Pending" 1.91:1) and **`--royal-blue` is not one either** (the legacy alias is re-pointed *lighter* in dark mode, which made the question bank’s row actions 4.21:1 there; 38 declarations rewritten). Every solid fill now has an explicit **on-colour**, because white on `--success-solid` is 3.77:1 and on `--warning-solid` 3.19:1. `--text-muted` moved one step darker: at `--slate-500` it was AA with no headroom on white and **failed at 4.28:1 on a tint**. **28 headings** were at the wrong level (eleven admin pages and six student ones opened with an `h3` under the shell’s `h1`, and the 404 page had no `h1` at all). The **44px touch floor now covers buttons under `pointer: coarse`**, tick boxes are 24px (WCAG 2.5.8) and ten sub-11px font sizes were raised. Information that existed **only on hover** — two disabled bulk actions and the five payment states — is now in the page. And **fifteen routes were not lazy**: the main bundle went from 543 kB (167 kB gzipped) to **244 kB (75 kB)**. **No API call was added, removed or changed** (100 endpoint references before and after) and no backend file was touched (1253 tests across 35 files, unchanged). Final state across 21 admin, 13 student and 6 public routes: no page overflow, no element escaping its container, no dangling ARIA reference, no duplicate id, no unnamed control, no heading skip, no emoji, no text under 4.5:1. Before that, **Milestone 23, Phase F (the landing page)** — the public page rebuilt on the design system, and **three claims removed because they are not true**: "there is no negative marking" (there is — `Question.negativeMarks` exists and `services/grading.ts` applies it), "results are published within 48 hours" (nothing promises or enforces that) and the hero’s "2027" (the certificate this product prints carries no year, and the only year in the system is the current one in a serial — **flagged for the owner rather than decided**). Six sections it never had were added: what you get, how it works, the ten class levels rendered from `CLASS_LEVELS`, three assurances that are properties of the system rather than adjectives, seven verified FAQs and a closing call to action. Two things it deliberately does **not** say: no entry-fee amount (there is no public endpoint for it, and adding one for a marketing page would be a backend change made for the UI) and no referral earnings (`rewardEnabled` defaults to false). **The last emojis in the product are gone** — `src/` and `index.html` now contain zero. **No API call was added, removed or changed** and no backend file was touched (1253 tests across 35 files, unchanged). Verified at 375, 768 and 1280px in both themes: no overflow, no dangling ARIA, no unlabelled control, heading order with no level skipped, all 45 Phosphor glyphs resolving, lowest text contrast 4.55:1. Before that, **Milestone 23, Phase E (the admin experience)** — 24 admin pages plus the AI generator. **Fifteen bare `<table>`s became `TableScroll` + `Table`**, one of which (platform analytics) had been pushing the whole page 445px past a 375px viewport, so every element on it could be dragged off screen sideways; **no column was dropped** to make anything fit, and the student directory — whose rows are records rather than comparisons — becomes one card per student below 768px with all thirteen fields. **`/ai-generator` rejoined the admin area**: it is in the admin navigation but rendered its own frame with a `← Back to Admin` link, so arriving from the drawer dropped you out of the shell. **The two multi-step flows now say where you are** — bulk import (Upload → Review → Saved) and AI drafting (Configure → Review → Saved) on a new shared `ui/Steps`, with the current step derived from what exists rather than tracked separately, and the middle one deliberately named *Review* because nothing has been written at that point. **An unmatched address is no longer a blank page**: there was no catch-all route and React Router renders nothing when nothing matches, so a typo produced a white page indistinguishable from a crash; `/*` now renders `pages/NotFound`. **Fifty-four hand-written `<i className="ph-...">` icons became `Icon`** across 25 files, which is the rule Phase A wrote down and could not enforce. **No API call was added, removed or changed** and no backend file was touched (1253 tests across 35 files, unchanged). Verified at 375px and 1280px across twenty-one admin routes: no page overflow, no element escaping its container, no dangling ARIA reference, no unlabelled control, no emoji, no console error; both themes measured, one contrast finding fixed. Before that, **Milestone 23, Phase D (the student experience)** — nine surfaces redesigned on the design system: the dashboard (reordered to lead with *what to do* rather than four figures, with the actions moved from the last card to the first section), practice (picker and runner), mock tests (listing and runner, with a **sticky countdown** and submission moved into a dialog), the daily challenge, analytics (charts now take their colours from the tokens and follow the theme, having carried three hex literals), recommendations, Refer & Earn, the profile, and the invoice section. Every runner now shares one shape — 56px option rows, a chosen state carried by border *and* tint *and* a filled key, 44px palette buttons, stacked navigation on a phone. Listings that are records become **cards below 768px** with no column dropped; listings that are comparisons keep a contained scroll. **All emojis are gone from every student surface**, medal emojis included — they are now an icon beside the rank rather than in place of it. **No API call was added, removed or changed** and no backend file was touched (1253 tests across 35 files, unchanged). Verified at 390px and 1280px across eleven routes including a live practice session: no page overflow, no dangling ARIA, no unlabelled field, no console error. Before that, **Milestone 23, Phase C (authentication)** — sign-in and registration extracted from the 749-line landing page into `pages/Auth/LoginDialog.tsx` and `pages/Auth/RegisterForm.tsx` (the page is now 256 lines); registration validates **every field in one pass and reports each message on its own field**, with a summary that moves focus, replacing the single first-problem string that sent a student with three mistakes round three times; a new `PasswordInput` with a show/hide toggle on all six password fields; `autoComplete` on every field so password managers and mobile autofill work; the administrator's sign-in fields **labelled for the first time** (`<label>` with no `htmlFor` beside `<input>` with no `id`); and four defects found in the browser — a raw zod aggregate printed as an error, a wrong password answered with "your session has ended", the Sign in link dying after one use, and a dangling `aria-controls`. All emojis gone from the auth surfaces. **No backend file was touched** (1253 tests across 35 files, unchanged). Before that, **Milestone 23, Phase B (the global layout)** — one `AppShell` for both signed-in areas over one navigation model (`components/layout/navigation.ts`), replacing two near-identical copies of a sidebar, a drawer and a topbar; flat lists of 16 and 20 links became five and six named groups; `/exam` joined the student navigation for the first time since it was built in Milestone 13, which also made the `paid` padlock render for the first time; three deliberate layouts (a student bottom bar below 768px, a burger from 768px, a permanent sidebar from 1024px); the public header dropped from eight links to four with the lookups and the admin door moved into a real footer, and **gained a Sign in button** it had never had. Four defects were found in the browser, three of them in code written that phase — the admin had no burger on a phone, focus did not move into the drawer, `inert` left the desktop sidebar unreachable, and a dark-mode group label measured 4.46:1. **No backend file was touched** (1253 tests across 35 files, unchanged) and no page's content was redesigned. Before that, **Milestone 23, Phase A (the design system)** — a token layer (`tokens.css` / `base.css` / `utilities.css`, replacing a 207-line `theme.css` that held thirteen colours and one `.card` class) and **twenty domain-agnostic primitives** in `frontend/src/components/ui`. **No page was redesigned in this phase and no backend file was touched** (1253 tests across 35 files, unchanged); what changed for existing pages is what they inherit — every legacy token name still resolves, the five global classes were kept and modernised, and `components/Button.tsx`, `Spinner.tsx` and `StatTile.tsx` became re-exports, so 88 imports picked up the new design untouched. The system exists because the shared layer under 12,450 lines of CSS was thirteen colours: there were **52 separately-declared `.status` badges, 66 `.table`s across 21 pages, 16 `.notice`s and 8 `.modal`s — none of which trapped focus**. Phosphor stays as the single icon library (no dependency added) behind one `Icon` component; Inter joins Poppins for interface text, with fewer font files than before. Four defects were found by driving a browser rather than reading: the modal never moved focus (`requestAnimationFrame` does not fire in a non-compositing tab), `Tabs` emitted `aria-controls` at panels not in the document, `TableScroll` kept a stale overflow measurement on resize, and a gold badge measured 3.69:1. Verified in both themes at 320/375/390/768/1280px with no page overflow anywhere. Before that, **Milestone 20 (the AI question generator on the official Gemini SDK, with structured output and review tooling)** — `@google/genai` replacing the hand-rolled `fetch` (which **supersedes** the Milestone 17 ADR that rejected an SDK; retries stay ours, bounded by `GEMINI_MAX_RETRIES`, and only for 429/5xx/timeout), a `responseSchema` built per question type so a numeric question has no `options` property to fill in wrongly, subtopic support validated before a request is spent, a **dry-run** endpoint that answers "would this save?" through the same screening approval uses, per-question selection and approval, **advisory** quality warnings that annotate and never reject, reviewer rejections recorded against the generation log, a `provenance` block on every saved question read back from our own log rather than the request body, and a 60/hour rate limiter on the one route that spends third-party quota. **The whole gate was run and is green: 882 tests across 26 files, typecheck, lint, compile, and the frontend build** — which also resolves the previous entry's unrun-changes warning. `npm audit` remains unrun. Before that, the **complete security audit** — five confirmed findings, five fixes: CSRF closed by an origin check mounted for the whole API, `localhost:5173` dropped from the production CORS allow-list, the two public lookups no longer publishing a child's full legal name, security headers on the frontend deployment, and rate limits on the payment and credential-issuing admin routes. That session could not run anything; **Milestone 20 ran it all and it passes.** Before that, **Milestone 19 (payments, and the entry fee that gates the platform)** — Razorpay Standard Checkout verified server-side, a entry fee that buys a seat in **the official Olympiad only** (₹100 at the time; **₹199** since 2026-08-28) (it gated practice, mock tests and the daily challenge for one day on 2026-08-16 and the owner reversed that the next day — preparation is free), an entitlement **derived from a captured payment rather than stored as a flag**, one `requireEntry` gate answering 402 on `POST /exams/:id/attempt`, reconciliation in place of a webhook, and an admin console for the money and the fee. The fake QR "I've Paid" step in registration is **deleted**. This was the last planned development milestone. Before that, **Milestone 18 (generated questions need a human before they exist)** — generation now saves **nothing** until an examiner approves it, the blank-template generator is deleted, the full parameter set (chapters, Bloom's, language, marks, option count, instructions) reaches the prompt, a fifth question type `fill_blank` is auto-graded against author-listed answers, near-duplicates are refused, and a `GenerationLog` records every run. Short answer was deliberately **not** added — see the ADR. Before that, **Milestone 17 (AI question drafting)** — the admin question generator can now be backed by Google Gemini, behind the same kind of seam Milestone 16 used. It is **the only AI integration in the product**, it sends **no student data**, and it is optional: with no `GEMINI_API_KEY` the button creates blank drafts exactly as before. A model's output is never trusted — the taxonomy comes from the request, every candidate passes the same schema and LaTeX safety checks a hand-authored question passes, failures are reported rather than repaired, and everything written is a draft. Before that, **Milestone 16 (intelligent performance recommendations)** — five kinds of recommendation (weak topics, strong topics, difficulty, practice, insights) derived from the Milestone 15 pipeline and the published question bank, behind a **swappable engine seam** an ML or LLM implementation can be registered into. The default engine is statistical and is **deliberately not called AI**: findings are asserted on a 95% Wilson interval rather than a raw percentage, every recommendation carries the counts it was derived from as a required field, and nothing may suggest a topic the class bank cannot serve. **No AI provider is integrated** — Gemini was evaluated and declined for now on cost and children's-privacy grounds; see the three Milestone 16 ADRs. Before that, **Milestone 15 (performance analytics)** — every one of the eight requested student figures (accuracy, topic, subject and difficulty performance, time and progress trends, weak and strong areas) derived from real submitted attempts, plus admin question- and paper-performance. The never-written `StudentAnalytics` model was **deleted** rather than filled in. Before that, **Milestone 14 (the notification system)** — an audit first, because Milestone 12 had already built the in-app half, which was left alone. What Milestone 14 added is delivery and automation: a new `EmailOutbox` so **no user-facing request ever waits on SMTP** (registration used to await the handshake inline and lose the verification link on failure, with no record and no retry), six **system-generated** notifications for real events, per-student notification **preferences** for the two optional email streams — with security and transactional mail deliberately not switchable — an opt-in capped email broadcast for staff announcements, and an admin console that makes a failed delivery visible with the provider's own error text. Before that, **Milestone 13 (the official exam and the certificate system)** — the thing the product is named after, and previously its biggest gap. A new `Exam` collection with a mandatory announced window; a rewritten `ExamAttempt` with one attempt per student enforced by a unique index and a clock the server owns; a rewritten `Result` released as a deliberate administrative act that ranks the cohort and mints certificates in one operation; and a `Certificate` that can **only** come from that release, with every printable field snapshotted, a server-rendered `pdf-lib` PDF, and public verification keyed on a random code rather than the readable serial. Before that, **Milestone 12 (Complete Admin Platform)** — twelve of fourteen areas, with eight pre-existing ones deliberately not rebuilt: an event gallery, in-app notifications, platform analytics where every figure is counted from a collection, an unmasked admin leaderboard, and a badges overview. Before that, **Milestone 11 (Account administration and a real super-admin account)** — the bootstrap super admin became a `Student` document with a rotating refresh token and a revocable `tokenVersion`, and staff gained password reset, forced sign-out, a `blocked` status and super-admin-only delete. Before that, **Milestone 10 (Leaderboards and Hall of Fame)** — one ranking service every standing goes through, boards scoped by class and by period, equal XP sharing a rank, pagination whose ranks survive a page boundary, and a depth cap that keeps the public board from being walked._

_**No endpoint or page returns placeholder data anywhere in the product.** That was achieved in the Milestone 5 follow-up and has held since. Milestone 13 went further and emptied the "honestly empty" category too: the result portal, the dashboard's exam panel and the certificate page were all written as live queries against then-empty collections precisely so they would start working when submission landed, and they did. **Milestone 15 closed the category entirely**: `StudentAnalytics` was the last model the product read and nothing wrote, and it was **deleted** rather than filled in — analytics are derived on read from the four attempt collections. Every remaining model is both read and written._

_**Verified counts** (read from the code, not carried forward, 2026-08-16): **26 models** (Milestone 19 added `Payment` and `PaymentSettings`; Milestone 18 added `GenerationLog`; Milestone 15 removed one), **22 permissions** (3 student / 19 admin / 22 super admin — the Milestone 22 content reset added `content:reset`, super admin only; **Milestone 19 added none**, reusing `students:read` to see payments and `students:status:write` to change the fee — **the security audit added none either**), **49 frontend routes** (Milestone 19 added `/payment` and `/admin/payments`), **28 services**, **24 route modules** under `routes/v1/`. **The test count is deliberately not stated**: it was 830 across 25 files at the end of Milestone 19, and the 2026-08-17 audit added `tests/security.audit.test.ts` without being able to run anything. Read it from `npm test --prefix backend`. Several rows below were carrying 14 or 17 permissions and an M10-era test count; if a number here disagrees with `backend/src/lib/permissions.ts` or `npm test`, the code wins._

| Feature | Frontend | Backend | Database | Testing | Notes |
|---|---|---|---|---|---|
| Registration | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | Real end-to-end: form → `/api/v1/auth/register` → `Student` + `StudentPhoto` docs → verification email. Now collects **13 fields** — first/middle/last name (`fullName` is derived), father's and mother's names, date of birth, class (ten fixed values: 5th–11th plus the three 12th streams), current school, full address, mobile, email, password — and a **mandatory photo** (≤2 MB, JPEG/PNG/WebP, validated by magic bytes, stored in `StudentPhoto`). Issues **no session** — the student must verify first. Unique `mobile`/`email`/`studentId`, bcrypt cost 12, rate limited 10/hour. 40 tests read the fields back out of MongoDB rather than trusting the response. **Redesigned in Milestone 23 Phase C**: extracted to `pages/Auth/RegisterForm.tsx`, and every field now reports **its own** error. One validation pass finds all problems; each renders under its field with `aria-invalid` and a described-by message; a summary at the top lists them as buttons that focus the field they name. It previously showed the first problem only, as one string at the top of a two-screen form. Plus `autoComplete` throughout, `inputMode="numeric"` for the mobile number, a photograph preview with a Remove action, and a real ordered step list with `aria-current="step"`. |
| Login | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `/api/v1/auth/login` accepts **mobile or email**. bcrypt compare, access + refresh cookies, account-status and verification gates, lockout after 5 failures (423), no account enumeration. Fully covered by integration tests. **Redesigned in Milestone 23 Phase C**: `pages/Auth/LoginDialog.tsx`, on the design system's `Modal` — focus moves to the first field, Tab is trapped, Escape closes, the page behind is scroll-locked, and on a phone it is a bottom sheet. The overlay it replaces had none of those. `autoComplete="username"`/`"current-password"` (the identifier takes either, so `username` rather than `email`) and a show/hide toggle. Empty fields are caught before the request, and `humanizeSignInError()` keeps a 401's non-enumerating message while replacing the 400's zod aggregate — both of which reached the screen during the browser pass. |
| Logout | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `/auth/logout` revokes the presented refresh token server-side (other devices stay signed in); `/auth/logout-all` revokes every session and bumps `tokenVersion`. Both tested. |
| Email verification | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | Real single-use, hashed, 24h token emailed via SMTP (`nodemailer`); `/verify-email` page + `/auth/resend-verification`. **Login is blocked until verified.** `VerificationToken` collection with TTL. **SMTP is configured and delivery is confirmed** (owner, 2026-08-12): verification links reach students' inboxes. This closes the item that gated login and admin promotion. `npm run verify:email --prefix backend` remains the deliberate way to re-check it. Since Milestone 7, `dev:local` **does** suppress mail — it points SMTP at a dead local port and turns verification off, so local work cannot email a stranger. |
| Forgot password | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `/forgot-password` page → `POST /auth/forgot-password`, emails a single-use 30-minute hashed token. Always returns an identical generic response so accounts cannot be enumerated (asserted by test). Rate limited 5/hour. |
| Reset password | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `/reset-password?token=` page → `POST /auth/reset-password`. Single-use token, revokes **every** session, bumps `tokenVersion`, clears lockout, marks the email verified. Tested incl. reuse and expiry. |
| Password reveal (show/hide) | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **Added 2026-08-29.** `ui/PasswordInput` — a real `<button>` with `aria-pressed` and an accessible name that changes with the state, on all six password fields in the product (sign-in, registration ×2, reset ×2, forced change ×3). Not a nicety on a phone: a password is typed on a soft keyboard into a field showing dots, and two of these forms ask for it twice, so the alternative to revealing it is retyping both. Revealing does not change `autoComplete`, so password managers still recognise the field. |
| Student profile | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Completed in Milestone 5.** New `/profile` page: view and edit the nine registration fields (`GET`/`PATCH /me/profile`), replace the photo (`PUT /me/photo`), and read the account metadata. `fullName` is re-derived server-side. `email` and `mobile` are shown read-only and are **absent from the update schema**, not merely filtered — they are the login identifiers and `email` anchors password reset, so changing one needs a confirm-at-the-new-address flow that does not exist yet (see `DECISIONS.md`). A test proves sending `email`/`role`/`status` changes nothing. 27 tests read the saved documents back. |
| Admin authentication | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | Two routes now. The env-configured **root** account (`POST /auth/admin/login`) holds `superadmin`, gets an 8h access token and **no refresh token** (no DB record to anchor a family to). **Promoted** admins are ordinary accounts with `role: 'admin'` and sign in through the normal `POST /auth/login`, inheriting lockout, verification, rotation and password reset. Both paths covered by `rbac.test.ts`. **Fixed 2026-08-13:** the `/admin` portal posted only to the root endpoint, so a promoted admin typing their correct password was told "Invalid admin credentials" — it now tries the root endpoint and falls back to the ordinary login on a 401, so one form serves both identities. |
| RBAC | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Rebuilt in Milestone 3, widened in Milestones 11, 12 and 13.** Three roles (`student`/`admin`/`superadmin`) and **22 named permissions** in one table (`lib/permissions.ts`) — a student holds 3, an admin 19, a super admin all 22. Only `users:role:write`, `users:delete` and (since Milestone 22) `content:reset` are super-admin-only. `SUPERADMIN_PERMISSIONS` is defined as a *superset* of the admin list, so "an admin can never do more than a super admin" is structural rather than a convention that could drift, and a test asserts the subset relation against the table itself; routes declare a permission via `requirePermission`, never a role. Privileged requests re-read the role from the database, so demotion/suspension bites immediately instead of at token expiry. Frontend guards and navigation are driven by the permission list the server sends. 62 tests drive escalation attempts directly at the API. |
| Student dashboard | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Rebuilt in Milestone 5.** Every figure comes from `GET /me/dashboard`; there is no constant left on the page. Shows XP and level with progress to the next, current and best streak, leaderboard rank, achievements earned, a real activity feed, available practice from the published bank for the student's own class, and recent test performance. The three invented stat tiles ("1,280 challenges solved today", "8.91s fastest solve", "450+ participating schools") and the three-name fake leaderboard are gone. Loading, error and per-panel empty states throughout. Opening it records the day's visit (idempotent, unique-indexed). **Redesigned in Milestone 23 Phase D.** Reordered to lead with *Jump back in* — practice, mock tests, today's challenge — because the page previously opened with four figures and ended, seven cards later, with the actions, which on a 390px screen put them below three screens of scrolling. Then progress (a real `value`/`max` level bar, and tiles where an unranked student gets an em dash rather than a zero), then the record. The recent-tests table is **cards below 768px**, a table above it, with no column dropped. All seven emoji headings and the `👋` are gone. **No request was added**: the action-card counts come from the `GET /me/dashboard` payload the page already had. |
| Recent test performance | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | The dashboard panel queries `ExamAttempt`, and **as of Milestone 13 that collection is genuinely written**, so the panel now shows real official-exam attempts instead of "No results yet". It was deliberately built as a live query rather than a hardcoded `[]` precisely so it would start working the moment exam submission existed — it did, and the shapes were updated in the same change. It still **excludes mock tests on purpose**: a mock is a rehearsal, not the Olympiad, and conflating them here is exactly the mistake the practice/official separation exists to prevent. A student who has sat mocks but no official exam still sees an empty panel here; their mock history is on `/mock-tests`. |
| Admin dashboard | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `Admin.tsx` now shows **real** figures — total accounts, administrator accounts, suspended accounts and the most recent registrations — all counted by `GET /admin/students`. The 4 fake student rows are gone. Sits in a shared `AdminShell` whose sidebar is permission-aware. **The sample weekly-accuracy chart was removed on 2026-08-11** and replaced with two real 14-day series from `GET /admin/stats` — registrations per day and active students per day, bucketed by IST day to line up with the streak and XP days. **Redesigned in Milestone 23 Phase E**: a permission-filtered **quick actions** grid leads the page, so the five things an administrator opens `/admin` to start are one tap away rather than behind the drawer; the recent-registrations table is a `TableScroll`; the figures are `StatTile`s and render an em dash rather than a zero for a figure that is genuinely absent. |
| App shell (student + admin) | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **Rebuilt in Milestone 23 Phase B as one component**: `components/layout/AppShell` renders the chrome for both signed-in areas, over one navigation model in `components/layout/navigation.ts`. `components/StudentShell.tsx` and `pages/Admin/AdminShell.tsx` survive as thin wrappers holding only what each half knows — the unread count and the entry-fee padlock; the permission filter and the identity block. Their `title`/`subtitle` props are unchanged, so all 29 pages that use them were untouched. **Three layouts**: a student bottom bar (Home · Practice · Tests · Challenge · More) below 768px, a burger from 768px, a permanent sidebar from 1024px (moved up from 768px because the admin area is wide tables). A timed paper passes `focus`, which drops the bottom bar — it sits where the answer buttons are — and keeps the burger. **The drawer is mounted only while open** and the sidebar is `display: none` below 1024px, so nothing about its correctness depends on an event arriving; it moves focus in, traps Tab, closes on Escape / the backdrop / an item / a route change, returns focus to its opener, and releases a shared counted scroll lock. A **skip link** is the first focusable element and `<main id="main-content">` is what it reaches. Replaces the Milestone 5 shell, whose links all pointed at pages that rendered the public navbar, so pressing any menu item made the menu vanish. |
| Navigation model | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **Added 2026-08-29.** `components/layout/navigation.ts` — the student and admin menus as data, behind the desktop sidebar, the drawer, the bottom bar and the permission filter at once. Grouped by what the reader came to do: Prepare / My progress / The Olympiad / Account, and Students / Question bank / Assessments / Insights / Communication / Settings. `findActiveItem()` matches by **longest prefix**, so a practice session keeps Practice Zone selected and `/admin/questions/import` selects Bulk Import rather than both it and Question Bank. Every entry points at a route that exists: there is deliberately **no admin Practice item** (practice is student-initiated — the administrative act is bulk-publishing in the Question Bank) and no general Settings page. **`/exam` was added**, having been reachable only from the dashboard since Milestone 13 — which is also why the `paid` padlock this model had always described had never rendered. |
| Landing page | IMPLEMENTED | IMPLEMENTED | N/A | NOT_STARTED (no frontend test suite) | **Rebuilt in Milestone 23 Phase F.** Hero, four real figures, what you get, how it works, the ten class levels, three assurances, the top three from the leaderboard, the registration form, seven FAQs and a closing call to action. Reads exactly three endpoints, all pre-existing: `GET /public/stats`, `GET /leaderboard?limit=3` and `GET /referrals/validate`. **Every claim is traceable to code** — three that were not (no negative marking, results within 48 hours, the year `2027`) were removed; see the ADR. It names **no entry-fee amount** (no public endpoint exists, and adding one for a marketing page would be a backend change made for the UI) and **no referral earnings** (`rewardEnabled` defaults to false). The class list renders from `CLASS_LEVELS`, so it cannot advertise a class registration would refuse. `/register` and `/register?ref=` still scroll to the form; `#login` still opens the dialog. |
| Public header and footer | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **Redesigned 2026-08-29.** The header carries four destinations (Leaderboard, Hall of Fame, Gallery, Verify a certificate), a theme toggle and one call to action; it had carried eight links in one row, including **Admin**, which a marketing page was advertising to every visitor. The result and certificate lookups and the administrator's door moved to a four-column footer with the helpline and support address as real `tel:` and `mailto:` links — nothing became unreachable. **A Sign in button was added**, closing a real gap: the login form is a panel on the landing page, so a visitor reading the leaderboard had no way to ask for it; it links to `/#login`, which the landing page opens on that hash. The mobile panel is mounted only while open, so there is no hidden second copy of the navigation in the accessibility tree. A promoted admin sees both Admin and Dashboard, because they hold `students:read` *and* have their own progress. |
| Theme (light / dark) | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **Added 2026-08-11.** `ThemeContext` toggles a single `theme-dark` class on `document.documentElement`, persisted in `localStorage`, **defaulting to light**. A `ThemeToggle` sits in the public navbar, the dashboard sidebar and the admin sidebar. Replaces the previous half-and-half state where public pages were light and the dashboard, admin area and exam each hardcoded dark, so signing in changed the colour scheme underneath the user. Background is painted on `html`, `body` and `#root` so pages without a full-height wrapper cannot show dark cards on a light canvas. Verified correct on load in both themes. **Live switching was confirmed on 2026-08-29** (Milestone 23 Phase A), and the earlier doubt is explained: adding `.theme-dark` does flip every token — `--bg`, `--surface`, `--text` and the painted `html` and `#root` backgrounds all change — but `getComputedStyle(body).backgroundColor` keeps reporting the *old* colour, because `body` and `.card` carry a `background-color` transition and a CSS transition cannot advance while the tab is not compositing. Removing the transition flips the reported value immediately. It is a measurement artifact of a hidden preview, not a `var()` substitution problem. |
| Design system (tokens + primitives) | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **Added 2026-08-29, Milestone 23 Phase A.** `frontend/src/styles/tokens.css` (palette → semantic → legacy aliases; type, space, radius, shadow, motion, one z-index ladder, `--tap-target`), `base.css` (element defaults, one global focus ring, `prefers-reduced-motion` honoured once) and `utilities.css` (six layout utilities + the five pre-existing global classes, kept and modernised). `theme.css` remains the single entry as three `@import`s, so `main.tsx` is unchanged. Plus **twenty domain-agnostic primitives** in `components/ui`: `Icon`, `Button`/`ButtonLink`, `Card`(+3), `Badge`, `Alert`, `Field`, `Input`/`Textarea`/`Select`/`Checkbox`/`SearchInput`, `Modal`, `ToastProvider`+`useToast`, `Tabs`+`TabPanel`, `Pagination`, `Skeleton`(+4 presets), `Table`/`TableScroll`/`DataCard`(+2), `EmptyState`, `ErrorState`, `Spinner`, `Progress`, `Tooltip`, `StatTile`, and `lib/errors.ts` (`humanizeError` — a 5xx message is never shown to a user). Several rules are held by the types rather than by review: a `Field` cannot exist without a label, an icon-only `Button` without an `aria-label`, a `Badge` without words, a determinate `Progress` without a real `value`. **Replaces nothing yet** — 29 pages still use their own modules and the legacy classes; Phases B–F migrate them. **Phase E added a twenty-first primitive, `Steps`** — extracted from registration's hand-built row so the bulk importer and the AI generator could say where you are the same way. It is a state display rather than a control: the steps are not links, `aria-current="step"` marks the position, and a step is `done` only once the work behind it is finished — a previewed import has written nothing and is still on Review. |
| Icon system | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | Phosphor 2.1.1 as a webfont, **pre-existing** (~87 distinct glyphs across 40 files) and kept — no dependency was added, and self-hosting the npm package was tried and reverted because its `@font-face` drags a 3 MB SVG font into the bundle. Milestone 23 Phase A put **one** `Icon` component in front of it: an icon with a `label` is `role="img"`, one without is `aria-hidden`, and the weight type admits **only `regular` and `bold`** because only those two stylesheets are loaded — `ph-fill` matches no `@font-face` and renders an invisible glyph rather than falling back. Known cost, recorded rather than mitigated: the font comes from unpkg at runtime, so **no icon may ever be the only carrier of meaning**. **Milestone 23 Phase E finished the migration**: the 54 remaining hand-written `<i className="ph-...">` sites across 25 files now go through `Icon`, so nothing in `src/` names a Phosphor class directly and the invisible-glyph trap is unreachable from a call site. **Milestone 23 Phase F removed the last emoji in the product** (the three leaderboard medals, the hero star, the trophy heading and the crown on the XP badge): `src/` and `index.html` now contain zero emoji characters, and a rank medal is an icon *beside* the number rather than in place of it. |
| Toast notifications | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **Added 2026-08-29.** `ToastProvider` mounted once in `App.tsx`, `useToast()` anywhere. At most three visible; each toast is its own live region — `role="status"`/polite, and `role="alert"`/assertive for an error, because a failed save is worth interrupting for. Verified in a browser. **Nothing calls it yet**: it is the target for the pages migrated in Phases C–F, and the rule travelling with it is that a validation failure belongs beside its field and a page caveat belongs on the page — a screen that toasts on every interaction trains people to dismiss them. |
| Design-system reference page | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **Added 2026-08-29. Development only** — `/design-system` is behind `import.meta.env.DEV`, so a production build has no route and no chunk for it (confirmed by grepping `dist/`). Renders every primitive in every variant with a live viewport read-out, which is how the responsive and both-theme checks are run given this frontend has no test suite. Contains **no product data and makes no API call**; its sample rows are labelled as samples. |
| Student management | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Widened in Milestone 11.** `/admin/users` now also offers **reset password** (a one-time temporary password shown once, which revokes every session and forces a change at next sign-in), **force sign-out**, a fourth status **blocked**, and — super admin only — **delete**, which refuses any account that has verified its email and asks the caller to retype the account's `AMIT_xxxx`. The super admin's own row is listed but shows *Protected*: `refuseIfProtected()` blocks every one of these against it. Real paginated listing showing **photo, class and school** alongside the existing columns, with search (literal, regex-escaped) and status/role filters, plus suspend / deactivate / reactivate and — for a super admin only — grant/revoke admin. Loading, error and empty states included. Backed by `GET /admin/students`, `GET /admin/students/:studentId`, `PATCH .../status`, `PATCH /admin/users/:studentId/role`. Suspension and role change end the target's live sessions immediately. **No delete route** — deliberately out of scope, deactivation is the reversible equivalent. |
| Question bank | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Rebuilt in Milestone 4.** Full admin CRUD at `/admin/questions`: create, edit, publish, archive, restore, delete-if-never-published. Four question types, options with server-assigned keys and `isCorrect` flags, marks + negative marks, `draft`/`in_review`/`published`/`archived` workflow, tags, revision counter. Search (literal, regex-escaped, across text/tags/solutions), filters on 7 fields, allow-listed sorting, stable pagination. 77 tests. **Security fix:** the student endpoint was previously unauthenticated and returned `correctAnswer` — it is now gated and answer-stripped. **Redesigned in Milestone 23 Phase E**: the listing is a `TableScroll`, the sort direction is an icon-and-word toggle rather than a bare arrow glyph, the bulk-status bar reads "Publish 3 for practice" rather than an arrow rebus, and the pager carries direction icons. |
| Subjects | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 4.** Real `Subject` collection with case-insensitive unique names, derived slugs, display order and archive status. Managed at `/admin/taxonomy`; readable by students via `GET /subjects` for filters. Archiving is refused while published questions reference it. |
| Topics | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 4.** Real `Topic` collection; `Question.topic` is a required reference, validated to belong to the stated subject. Uniqueness is scoped to the parent, so the same topic name may exist under two subjects. |
| Subtopics | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 4.** The *same* collection as Topics, distinguished by a nullable `parent` and a derived `depth` capped at 1 (topic → subtopic, no deeper). Optional on a question, and validated to belong to the selected topic. Rendered as a nested tree in the admin UI. |
| Question types | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 4.** `single_choice`, `multiple_choice`, `true_false`, `numeric` (with optional tolerance). Each uses exactly one answer representation, and validation **rejects the fields belonging to the other types** so no unreadable data can be stored. |
| Marks / negative marks | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 4.** Per-question `marks` (0.25–100) and `negativeMarks` (a magnitude to deduct; 0 disables it, cannot exceed `marks`). Stored and validated, and **now actually scored against** — by the Practice Zone (Milestone 6) and by mock tests (Milestone 7), which may additionally override both per test, because the same question is legitimately worth 2 marks on a quiz and 6 on a final. The *official* exam still does not exist. |
| Mathematical content | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 4.** LaTeX islands in plain text, rendered by KaTeX. Prose renders as React text nodes and only KaTeX output enters the DOM as HTML, so author text never reaches an HTML sink. Backend rejects link/file/macro commands, unbalanced delimiters, markup and control characters. Verified in a real browser: a `<script>` in a question yields zero script elements. |
| Daily challenge | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Completed in Milestone 8** — it was a read-only card before. `/daily-challenge` shows today's question for the caller's class, takes one answer, marks it server-side and reveals the correct answer and the author's explanation immediately, with a streak and a history. **The day is pinned to a `DailyChallenge` document** rather than recomputed, which fixed a real defect: the old `hash(day) % publishedCount` picker changed which question "today" was whenever anything was published, and made a past day unrecoverable. **One reward per IST day, guarded twice** — a unique index on `DailyChallengeAttempt {student, day}` and the once-per-day cap in `recordActivity()` — so a repeat submission returns the stored answer, `alreadyAnswered: true` and `xpAwarded: 0`. Worth 15 XP for answering (not for being right; wrong answers are never penalised). **Staff can schedule** days in advance at `/admin/daily-challenges` under the new `challenges:write` permission, with per-day attempt and correctness figures; an unscheduled day fills itself automatically and is labelled as such. 37 tests. |
| Practice zone | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 6, real end-to-end.** `/practice` picks a subject, topic and difficulty from **real per-topic counts** (a combination with nothing behind it cannot be selected); `/practice/:id` runs the paper with free navigation, per-answer autosave and a palette, then shows a server-marked review with correct answers, explanations and a performance summary. `PracticeSession` persists the session, questions served, answers, correctness, per-question marks, score, time and completion status. **Answer integrity:** the in-progress view is answer-stripped, the reveal function throws on an unsubmitted session, and grading is server-side — no answer key reaches the browser, asserted by tests that stringify whole response bodies. Submitting earns 25 XP once per competition day, so it cannot be farmed. 42 tests. |
| Mock tests | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 7, real end-to-end.** Still distinct from the *official* exam below. **Admin** (`/admin/mock-tests`, gated on a new `mocktests:write` permission): create a test, pick questions from the published bank for its class, price each one for this paper, set duration / attempt limit / availability window / instructions, choose independently when students see their score and when they see the answers, publish / unpublish / archive, and read a ranked results table with cohort statistics and per-question outcomes. **Student** (`/mock-tests`): see the tests set for their class with the window and attempts left, start (or resume), work with free navigation and a palette, per-answer autosave, a server-derived countdown, submit — or be submitted automatically when time runs out — then see whatever the test's settings release. **Timing is enforced by the backend**: `expiresAt` is computed and stored at start, an answer after it is refused with 409 and not stored, and a late submission is graded *as at the deadline*. **Exactly one submission**, guaranteed by a conditional write plus a unique index on `{test, student, attemptNumber}`. Earns 50 XP once per competition day. 54 tests. |
| Exam attempts (official) | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Built in Milestone 13** — see the "Official exam" row below, which is the same feature. This row previously read NOT_STARTED and described `/exam` as a redirect to `/practice`; both are obsolete. Kept as a pointer so anybody searching for "attempts" lands somewhere true. The history is worth knowing: the page was once five hardcoded questions each carrying its `correct` answer, so the paper's answer key shipped in the JavaScript bundle. It became a real-but-unmarked practice run in the 2026-08-11 de-faking pass, and an actual scored official sitting in Milestone 13. |
| XP | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 5.** Real, and **derived rather than stored**: the total is a `$sum` over the new `StudentActivity` log, so there is no counter that can drift from the events behind it. Earned only from events that really happen — account created (50), email verified (50), daily visit (10, once per competition day). Profile/photo/password changes are recorded but worth **0**, because they are repeatable at will. Milestone 6 added `practice_completed` (25), Milestone 7 `mock_test_completed` (50) and Milestone 8 `daily_challenge_completed` (15), all **once per competition day** so none can be farmed. **Milestone 9 put every grant behind one engine** (`services/rewardService.ts` → `grantReward()`): routes supply facts about what happened, the engine decides eligibility and amount, and `recordActivity()` remains the only writer. Amounts are now administrator-tunable within 0–500 — and **cannot re-price history**, because each row stores what its event was worth at the time. No *official* exam XP yet, because no official attempt is recorded anywhere. A new verified account holds exactly 110, and a test pins that. |
| Levels | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 5.** A pure function of XP (`lib/xp.ts`) — deliberately not stored, so it cannot disagree with the XP. Ten tabulated thresholds then a linear extension, so levels are unbounded. The dashboard shows the level, the position inside it and the XP remaining. |
| Badges | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **A distinct concept since Milestone 9** — before that this row said "delivered as Achievements", which was one idea under two names. A badge is a **family held at a tier** (bronze / silver / gold) that keeps levelling as the student does more of the same thing; an achievement is a one-off goal that stops changing once earned. Five families in `lib/badges.ts`: Scholar (XP), Regular (streak), Practitioner (practice sessions), Test Taker (mock tests), Daily Solver (challenges). Derived on every read from the shared `RewardFacts` — nothing is stored as held, so a tier cannot be granted by a bug or go stale. Boundary cases are unit-tested: a value *equal* to a threshold holds that tier, and a value past gold does not overfill the bar. |
| Achievements | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 5.** Eight achievements in a code catalogue (`lib/achievements.ts`), evaluated against real facts on every read — nothing is stored as "earned", so it cannot be granted by a bug or go stale. Locked ones carry **real progress** (`2/3`), capped at the target. Deliberately **no exam or accuracy achievement**: with no exam data, such a row would be permanently unearnable with a bar that can never move — a fake statistic wearing a lock icon. A test asserts no achievement code contains `exam` or `accuracy`. **Milestone 9 moved the facts** to the shared `RewardFacts`, so achievements, badges and the journey are three pure functions of one object and none of them reads a database. **Milestone 8 added two**, `challenge_first` and `challenge_streak_5` — the first in the catalogue that need the student to have *answered* something, and admissible under the same rule only because daily-challenge attempts are now recorded. The catalogue reads no database: the challenge service supplies two counts on `ProgressFacts`. **Ten achievements.** |
| Streaks | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 5.** Current and longest run of consecutive days, computed from the distinct days in the activity log. A day is an **IST** calendar day, not a UTC one (`lib/competitionDay.ts`) — UTC would file a 00:30 IST visit under the previous day and break a streak the student had actually kept. A streak stays alive while the last visit was yesterday, since today is not yet lost. |
| Journey map | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 9.** Nine ordered stages from *Enrolled* to *Olympiad ready*, on `/rewards`, with exactly one marked **next** — the first incomplete one. It answers the question the other two catalogues do not: what should I do now? Every stage is a pure predicate over `RewardFacts`, and stages measure **cumulative** facts (`longestStreak`, never `currentStreak`) so a broken streak cannot un-complete a stage the student really reached — a journey that walks backwards would read as the site taking something away. `currentStageId` is null for a finished path and the page says so. |
| Leaderboards | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Made real in Milestone 5; became a feature in Milestone 10.** Still **derived, never stored** — no `Leaderboard` model exists, and a board is an aggregation over the same `StudentActivity` log XP comes from, so it cannot drift from the totals it ranks. `services/leaderboardService.ts` is now the **only place a rank is decided** (it moved out of `progressService.ts`), used by all four surfaces that show one. **Scopes**: overall and per class — a class board ranks *within* the class. **Periods**: all time / last 30 / last 7 / today, measured in IST competition days, ranking on XP earned *inside* the window. **Pagination** with ranks that survive a page boundary, including a tie split across one. **Tie-breaking is defined**: equal XP shares a rank (1, 2, 2, 4), and the order within a tie is XP → who reached it first → account id, a total order so the same query always returns the same sequence. Suspended and deactivated accounts excluded *before* the limit. A student with no XP in the window is genuinely **unranked** (`rank: null`), as is one viewing a class that is not theirs. **Public by the owner's explicit decision**, publishing a first name plus a last initial only; a signed-out visitor may page the **top 100** only (403 beyond), which restores the anti-enumeration property pagination would otherwise have removed. **No client-supplied value is trusted**: XP/rank/score are absent from the query schema, not filtered by the handler. New `/leaderboard` page, public. 49 tests. |
| Certificate page (`/certificate`) | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | The public single-certificate view, distinct from the student library and the admin console in the "Certificates" row below. **De-faked 2026-08-11**, then made genuinely non-empty by Milestone 13. The page used to print "For outstanding participation and achievement" for anyone signed in (and "Future Champion / AMIT_XXXX" for a guest), dated today, with the student ID as the certificate number — a printable credential nobody had earned. It now calls `GET /certificates/:studentId`, which was itself a hardcoded two-item mock, became a real query requiring a **published result**, and since Milestone 13 returns really-issued certificates. |
| Notifications (in-app) | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 12, extended in Milestone 14.** Staff compose at `/admin/notifications` under `notifications:write`, with drafts, publish/withdraw, audience targeting, pagination, filtering and a real **`readCount`**. Students get `/notifications` with real read state, an unread count and "unread only" — plus, since Milestone 14, an **unread badge on the sidebar**, a **source badge** distinguishing automatic notices from staff-written ones, and a **deep link** to whatever the notice is about. **Nothing is fanned out**: one document carries an audience *rule*, and each inbox is that rule evaluated at read time, so a student who registers tomorrow still sees today's announcement. Read state is its own collection with a unique index, which is what makes "mark as read" idempotent. Deleting an announcement takes its receipts with it, or the unread anti-join would be wrong for everyone who had read it. **Fixed in Milestone 14:** the mark-as-read route was hand-writing its own audience comparison — correct for the two audiences that existed, and it would have silently refused every per-student notification the new milestone added. It now composes `inboxFilter()` through `isVisibleTo()`, so the inbox, the unread count and the read check share one definition of visibility. The admin list now **defaults to the staff stream**, because releasing one exam's results writes a system row per candidate and would otherwise bury the composer's own announcements. |
| System notifications | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 14.** Six real events now notify without a human typing anything: an official exam published (class-wide, in-app only), **results released** (per candidate, emailed), a mock test published (class-wide, in-app only), and account **status** / **role** / **password** changes (per student, emailed as `security`). `lib/systemNotifications.ts` holds the catalogue — copy, email category and opt-out rule — as pure data with no database access, the same discipline as the achievement catalogue; `services/systemNotifier.ts` is the only layer that turns a domain event into a notice. `Notification` gained an `audience: 'student'` that **staff cannot address** (absent from the composer schema, not filtered by the handler), because a per-student notice carries a score and a rank. Results and certificates produce **one** message, not two, since a certificate can only come from a result release — and it is built by reading back the `Result` rows that were really written, so it cannot disagree with the portal it links to. Every administrator-triggerable event carries a **partial-unique `dedupeKey`**, so clicking "release results" twice does not tell the cohort twice. A system row **cannot be edited** (409) but can be deleted. |
| Email notifications | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 14**, and the reason the Milestone 12 "in-app only" decision is superseded rather than reversed: its reasoning (emailing the whole roll from a free tier is a deliverability problem, and the addresses are often parents') still governs, so email arrived as an **escalation of the inbox rather than an alternative to it**, under three constraints. **In-app is always written**, so declining an email never costs a student the message. **A broadcast is emailed only when staff deliberately tick it**, per announcement, unchecked by default and reset after every send, capped at 500 recipients with the cap reported. **Optional streams are switchable.** No new dependency and no new environment variable — the transport was already provider-agnostic SMTP, so Brevo, Resend, SendGrid, a Gmail app password or Mailtrap all work through the existing `SMTP_*` vars. |
| Non-blocking email delivery | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 14, and the most consequential fix in it.** `sendEmail()` was **awaited inline** in registration and forgot-password and **swallowed failures**, so a slow provider slowed every new student and a dead one *destroyed* the verification link they needed in order to log in — no record, no retry, one log line. Now every message is written to `EmailOutbox` **before** anything tries to send it, and the request returns after one indexed insert. `deliverEmail()` replaces `sendEmail()` and **throws**, which is what makes the queue possible at all. Claiming a row is a **conditional write with a visibility timeout** rather than a `sending` status, so a frozen serverless container leaves a message due again rather than stuck for ever; the honest cost is at-least-once delivery. Retries back off (1 min → 5 → 30 → 2 h) and give up terminally. This also closed a **timing oracle**: `forgot-password` returned an identical *message* for a known and unknown address but awaited a real SMTP round trip only for the former, leaking the very fact the identical wording hid. **26 of the 46 new tests are about failure**, including a recovered provider that genuinely delivers on a later attempt and two concurrent drains that cannot double-send. |
| Notification preferences | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 14.** Two switchable email streams (`announcements`, `results`) on `/profile`, saved per switch with no confirm step, stored as an embedded `Student.notificationPrefs` rather than a 25th model. **Two streams deliberately cannot be switched off:** `transactional` (verification and reset links are the mechanism of using the account) and `security` (a password or status change is how somebody notices a compromise — an option to silence it only ever helps an attacker). Those are **absent from the schema**, not ignored by the handler, and the API returns them **with their reasons** so the page states them rather than leaving the reader to wonder. `emailAllowedFor()` is the only place a preference is interpreted, and its rule order matters: the category check comes **before** the account-status check, so a *suspension notice* still reaches the account it suspends. A missing preferences object reads as all-on, matching what pre-Milestone-14 students were already getting. There is deliberately no `certificates` switch, because nothing sends that stream on its own. |
| Email delivery console | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 14**, at `/admin/email-deliveries` under the existing `notifications:write`. Exists because **a queue nobody can see is a queue nobody trusts**: before this, "did that student ever get their verification link?" was unanswerable by anyone. Lists every message with its status, attempt count and **the provider's own error text**, filterable by status and category, with counted statistics including `oldestPendingAt` — the only figure that answers "is the queue stuck?", which a bare pending count cannot. Two actions: **send queued now** (necessary rather than convenient, since the free tier has no scheduler and lazy draining cannot promise a delivery time on an idle site) and **requeue failed** (the counterpart to a terminal give-up, so a corrected SMTP setting does not need a database edit). The subject is listed; the body never is. |
| Gallery | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 12.** Public `/gallery` page (reachable without an account, like the leaderboard) plus admin CRUD at `/admin/gallery` under a new `gallery:write` permission, with staff-chosen display order, archive/publish, pagination and literal search. Images are stored **in MongoDB**, capped at **1 MB**, validated by **magic bytes** via a shared `imageDataUrl()` validator extracted from registration — so a file that merely claims to be a PNG is refused. `data` is `select: false`, so a listing never drags image bytes into memory. Archiving stops the **bytes** being served, not just the listing. **Storage caveat worth knowing:** this is the second tenant of a 512 MB free tier after registration photos — see `PROJECT_STATE.md`. |
| Official exam | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Built in Milestone 13** — the thing the product is named after, and previously the biggest gap. New `Exam` collection with a **mandatory announced window** (unlike a mock test's nullable one), authored at `/admin/exams` under a new `exam:write` permission. Students sit it at `/exam` with **one attempt only**, enforced by a unique index on `{exam, student}` rather than by counting. The server owns the clock: `expiresAt` is computed once and never recomputed, an answer after the deadline is refused and **not stored**, and a late submission is graded as at the deadline. Submitting shows **no score** — a score is not a result. `ExamAttempt` and `Result` were both rewritten from their pre-Milestone-4 shapes; nothing had ever written them, so there was nothing to migrate. |
| Results (official exam) | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Built in Milestone 13**, replacing the empty-by-design state. An administrator releases results once the window has closed; publication sweeps expired attempts first (so an abandoned paper is graded and ranked rather than silently dropped, which would flatter every other rank), computes rank, total candidates and percentile, and writes published `Result` rows. **Equal scores share a rank** (1, 2, 2, 4), the same rule the leaderboard uses. Idempotent — republishing updates the same rows. Students see nothing until release, and the public result portal at `/results/:studentId` is finally real. |
| Certificates | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Built in Milestone 13.** Issued **only** by releasing an official exam's results — there is no issuance route at all, which is what makes the frontend unable to manufacture eligibility: it is never asked. Tiered participation / merit / distinction from **per-exam** thresholds, snapshotted so re-tuning them later cannot change an old certificate. Server-rendered PDF via `pdf-lib` (free, local, no headless browser) with a **gold Amit Kumar signature** and a **generated AMIT OLYMPIAD seal** drawn from primitives. Two identifiers: a readable serial and a separate 16-symbol verification code, because keying verification on the serial would let anyone walk the numbers and harvest every entrant's details. Student library at `/my-certificates`, admin management at `/admin/certificates`, revocation that never deletes. |
| Public certificate verification | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 13.** `GET /verify/:code` and the `/verify` page are **public and unauthenticated** — a school, parent or employer must be able to check a document without an account. Everything shown is the certificate's own snapshot, so it confirms the paper in somebody's hand rather than what the live records say today. A revoked certificate reports as **revoked, not missing**; a forged code reports not-found and leaks nothing. Codes are accepted however they were typed off the paper. |
| Hall of Fame | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 10.** A dedicated public page at `/hall-of-fame`, backed by `GET /hall-of-fame` and `services/hallOfFameService.ts`. **Five boards measuring five different things**, because XP measures participation more than ability and a hall of fame that only re-ranked it would be the leaderboard with a nicer heading: XP champions (the leaderboard's own first page, reused so they cannot disagree), **best papers** (highest mock-test score as a *percentage* — 40/40 on a quiz and 40/80 on a final are not the same feat; papers at or below zero excluded), **streak legends** (`longest`, never `current`, so a broken run withdraws nothing; minimum two days), **challenge champions** (correct answers only — the XP is paid for answering, so "answered" would be another participation count), and **practice devotees** (submitted sessions, not started). **A board with nothing behind it is returned empty with a reason naming what would fill it** — never padded with a placeholder. Same name masking and same exclusion of accounts not in good standing as the leaderboard, through the same function. **No official-exam board**, deliberately: that competition has not been run. Still no notion of a *past* competition's hall of fame, which needs published results to exist first. |
| Student analytics | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Made real in Milestone 15.** Its history is the product's clearest example of the no-fabrication rule: it once returned 88% accuracy over 450 questions, a learning curve, four topic breakdowns and "top 5% of all national Olympiad participants" to *every* student (deleted in the Milestone 5 follow-up), then sat honestly empty because the only real figure was XP per day. All eight requested figures are now derived from **submitted** attempts across practice, mock tests, the daily challenge and the official exam: overall **accuracy**, **topic**, **subject**, **difficulty** and question-type performance, **time trends**, **progress trends**, and **weak/strong areas**. `services/analyticsService.ts` is the single derivation, and the page computes nothing. **Raw counts are summed and percentages derived last**, so combining 1/1 with 1/9 gives 20% rather than the 55% an average of percentages would give (there is a test). **`null` is never rendered as `0%`** — "answered nothing" and "got everything wrong" stay distinguishable. Authorization was already permission-based and tested; a non-existent student ID 404s. |
| Performance recommendations | IMPLEMENTED | IMPLEMENTED | N/A (derived on read) | TESTED | **Milestone 16.** Five kinds — weak topics, strong topics, difficulty guidance, practice suggestions and insights — from `GET /analytics/:studentId/recommendations`, rendered on `/analytics`. Built on the Milestone 15 derivation plus the **published bank for the student's own class**, so nothing can suggest a topic with no questions behind it; a weakness in a topic published only for another class is reported with no link rather than silently dropped. **Findings are asserted on a 95% Wilson interval, not a percentage**: 2 of 5 (40%) is *not* a weakness while 30 of 80 (37.5%) is, a perfect five is *not* a strength, and both are pinned by tests — this is what stops one lucky or unlucky session becoming a diagnosis. Every recommendation carries a **required `basis`** with the counts, the accuracy and the interval, shown on the card behind "Show the numbers", because advice a reader cannot check is indistinguishable from advice that was invented. The difficulty section cannot contradict itself: a level flagged for consolidation suppresses any step-up above it. Nothing is stored — there is no recommendation collection, for the reason `StudentAnalytics` no longer exists. 35 tests. |
| Recommendation engine seam | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | **Milestone 16.** `lib/recommendationTypes.ts` is the entire contract an alternative engine implements: implement `RecommendationEngine`, call `registerRecommendationEngine()`, set `RECOMMENDATION_ENGINE`. Nothing about the route, the response shape or the page changes. Three properties are enforced rather than documented: an engine **cannot query a database** (it is a pure function of a facts object, like the reward catalogues), an engine **cannot describe itself** (the service stamps `engine`, `generatedAt` and `hasData` from the registry entry it invoked — a test smuggles all three onto a draft and asserts they are ignored), and an engine that **throws** is caught so the statistical engine answers instead. An unknown configured id logs once and falls back. |
| AI / LLM integration | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **The only one in the product: question drafting** (Milestone 17, reworked in 18, rebuilt on the SDK in 20). `services/geminiQuestionGenerator.ts` calls Google Gemini through the official **`@google/genai`** SDK — the project's first new runtime dependency since Milestone 1, added on the owner's instruction, superseding the Milestone 17 decision against an SDK. **Structured output**: a `responseSchema` per question type, with the batch size and option count pinned by `minItems`/`maxItems` and `marks` deliberately absent (the paper is priced by the examiner). **Optional**: with no `GEMINI_API_KEY` the page says so and the endpoint answers **503 naming the variable** — it does *not* invent filler, since the blank-template fallback was deleted in Milestone 18. **No student data is sent** (a subject name, chapter names, an optional subtopic name, a class, a difficulty, the marks and the examiner's instruction — asserted by a test). **Not trusted**: the taxonomy comes from the request, every candidate passes `createQuestionSchema` including `validateMathContent()`, near-duplicates are refused, failures are rejected *and reported* rather than repaired, and **nothing is saved until an examiner approves**. Retries are ours and conservative (429/5xx/timeout only, `GEMINI_MAX_RETRIES`, one shared time budget across attempts); provider errors reach the examiner in Google's own words with the API key scrubbed out. `kind: 'model'` is recorded in the audit trail per batch and in each question's `provenance`. Recommendations still use **no AI** — see the Milestone 16, 17 and 20 ADRs for why the cases genuinely differ. |
| AI question review workflow | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 20.** On `/ai-generator`: subtopic picker, per-question tick boxes, inline editing, regenerate one or all, discard, **Check before saving** (a dry run against the same screening function approval uses — no provider call, so it may be pressed freely), approve the selection as drafts or published, and advisory warnings shown per card and per batch. Rejections are counted against `GenerationLog.rejectedByReviewer`. `Question.provenance` records the generator, the exact model, the log row, whether the reviewer edited it and who approved it — **read back from our own log, never from the request body** — and the question bank prints a badge from it and can filter on `source`. Nothing here claims the mathematics was verified; the review step is mandatory precisely because nothing can. **Milestone 23 Phase E** moved the page inside `AdminShell` (it had rendered its own frame with a text link back, so reaching it from the drawer left the admin area) and gave it a Configure → Review → Saved step indicator, one-column configuration on a phone, and tokens in place of the four hardcoded rgba colours in its stylesheet. |
| ~~AI performance analytics~~ | REMOVED | REMOVED | N/A | N/A | **Deleted in Milestone 15**, along with the `StudentAnalytics` model it wrote into. `generateAIInsights()` was rule-based string assembly (never an LLM), it mutated a field it never saved, and its only branch was unreachable. What replaced it is better and honest: **weak and strong areas** derived from real counts with a stated minimum sample of five answers, so the page reports measurements rather than prose. **No AI provider is integrated anywhere in the codebase.** The **generator** page is a separate thing and remains a template filler, not AI; since Milestone 4 it requires real subject/topic ids and writes drafts only. |
| Question performance (admin) | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 15.** The view staff did not have: which questions are actually working. A question nobody gets right is usually **mis-keyed or mis-tagged** rather than genuinely hard, and until now the only way to find one was for a student to complain. `/admin/performance` under the existing `analytics:read:any`, counted from every submitted attempt across all four surfaces and merged by summing raw counts, so a question used in both practice and a mock test reports one combined figure. Hardest-first by default, filterable by class, difficulty and subject, with a **skip rate** beside the accuracy — a mostly-skipped question is a differently-broken question from a mostly-wrong one. A `minAnswered` floor (default 3) stops one wrong answer topping the list for ever; it is a parameter, not a hidden constant, and the value in force is returned with the result. |
| Test performance (admin) | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 15.** Every mock test and official exam with at least one attempt, in one comparable table — where `testResults()` has always shown a single test's cohort in detail. Per paper: attempts started and submitted, **completion rate**, distinct students, mean, **median**, range and pace. The median sits beside the mean deliberately: on a cohort of a few dozen, one student who submitted a blank moves the mean several points, and that is exactly the case an invigilator wants to see rather than have smoothed away. `kind` marks every row, so a rehearsal can never be read as the Olympiad. |
| AI question generation + review | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 18.** `/admin/ai-generator` configures class, subject, **multiple chapters**, difficulty, question type, **Bloom's level**, **language**, marks, negative marks, **option count** and free-text instructions; Gemini writes the questions; the review screen offers **edit, regenerate one, regenerate all, delete**, then *approve as drafts* or *approve and publish*. **Nothing is saved until approval** — candidates exist only in the browser, and tests assert the question collection is still empty after generating. Approval **re-validates from scratch**, because the review screen is a client. Near-duplicates (80%+ word overlap with the chapter or the batch) are refused. Every run is recorded in `GenerationLog`. Practice questions need no separate feature: the Practice Zone draws from the published bank, so approving here is what fills it. |
| Fill-in-the-blank questions | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 18.** A fifth question type, auto-graded by the one grader against author-listed `acceptedAnswers`. Normalisation forgives capitalisation, spacing and a trailing full stop — and nothing else, because a grader that guesses can silently mark a wrong answer right. Students answer it in practice, mock tests, the daily challenge and the official exam; the review shows every accepted spelling so a student marked wrong can see what would have counted. **Short answer is deliberately absent** — it cannot be auto-graded without a human marking queue or sending a child's writing to a model. |
| AI mock-test generation | NOT_STARTED | NOT_STARTED | N/A | N/A | Requested in the Milestone 18 brief and **not built**. The pipeline it needs exists and is provider-agnostic; what remains is assembling a `MockTest` around a validated batch (difficulty and question-type distribution, total marks, time limit) and reusing the review screen. |
| Daily-challenge automation | NOT_STARTED | NOT_STARTED | N/A | N/A | Requested in the Milestone 18 brief and **not built**. Needs a config document, a selection engine avoiding recently-used questions, a secured trigger endpoint driven by a free external pinger (Vercel Cron is paid — see known bug #41), and an admin console. The daily challenge itself is real and working; only the *automation and AI generation* of it are outstanding. |
| Bulk question population | NOT_STARTED | NOT_STARTED | N/A | N/A | Requested in the Milestone 18 brief and **not built**. Blocked on content rather than code for nine of ten classes: only `Class 12 - Science` has subjects and chapters, so there is nothing to generate against elsewhere until a syllabus is supplied. |
| Payments | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 19, and the last planned development milestone.** Razorpay Standard Checkout at `/payment`, verified server-side; the static QR image and its "I've Paid — Create My Account" button are **deleted** (they recorded nothing, verified nothing, and created the account either way, so every student who registered was told something untrue). **₹199, once** (raised from ₹100 by the owner on 2026-08-28), administrator-editable at `/admin/payments` — a price is business configuration, so changing it is not a redeploy and leaves an audit entry naming who changed it and from what. **The browser is never believed about money**: `POST /payments/orders` takes no body at all, the amount comes from the settings document and the student from the token, and both capture paths verify an HMAC signature in constant time against a secret the bundle has never seen. **Capture is idempotent by conditional write**, because the return journey and the webhook routinely arrive in the same second and on serverless land in different invocations — a duplicate webhook changes nothing, a late `payment.failed` cannot revoke a capture, and a webhook for an order this server never created is acknowledged and dropped rather than creating a payment belonging to nobody. **Reconciliation stands in for the webhook** (the owner configured no webhook secret): `POST /payments/reconcile` asks Razorpay directly and captures on the same path, which is *more* trustworthy than a webhook because the answer comes from an authenticated call we initiated. **No SDK and no new dependency** — `fetch` and `node:crypto`, following the Milestone 17 precedent. 31 tests, none touching the network, with genuine signatures throughout. **Not yet driven through a real browser** — no Razorpay credentials exist in the development sandbox; see `PROJECT_STATE.md` "Immediate Next Task". |
| Olympiad entry fee (gate) | IMPLEMENTED | IMPLEMENTED | N/A (derived on read) | TESTED | **Milestone 19.** The fee buys **a seat in the official Olympiad and nothing else** (owner decision, 2026-08-17). Practice, mock tests, the daily challenge, analytics, the dashboard, the leaderboard and rewards are **free** — a student prepares for free and pays only to compete, and **paying later is the normal path**, so the fee is offered on `/payment`, as a dashboard banner and as a card on `/profile`. It briefly gated all four preparation surfaces on 2026-08-16; the owner reversed that the next day and the tests now assert the free surfaces are *not* gated, in both directions, so a paywall cannot quietly widen. **The entitlement is derived, never stored** — "has a captured `Payment` with purpose `olympiad_entry`", with deliberately no `hasPaid` flag on `Student`, for the reason XP and the leaderboard are derived: a stored boolean is a second source of truth about money. **One gate, mounted rather than called**: `middleware/requireEntry.ts` on the exam attempt route, so no surface can be added that forgets to ask. It answers **402, not 403**, because "not yet, and here is the button" and "never" are different messages and the frontend branches on exactly that. **It runs before the resource is looked up**: the check originally sat inside `startExamAttempt()` *after* its 404, so an unpaid caller could tell a real exam id from an invented one by the status code — fixed, with a regression test. `entryFeeEnabled` **defaults to `true`** and remains switchable, because a provider outage during an exam window needs a response that is not "nobody can enter". |
| Payments console (admin) | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 19**, at `/admin/payments`. Exists because without it the fee could only be changed by a direct database edit, and "has anyone actually paid?" was unanswerable from the app. Counted totals per status and the collected total — from an aggregation over `Payment`, never estimated, following the rule every other admin figure follows — plus the 100 most recent transactions with the student, the amount, the status, the method, the order id and Razorpay's own failure text. The fee form carries the **`entryFeeEnabled` switch**, which is the control that makes the paywall real. Reading is gated on `students:read` (it exposes who paid what, which is student account data); **changing** the fee needs `students:status:write`, because seeing a price and setting it are different acts. **No new permission** was added. Warns plainly when the Razorpay credentials are missing, since a fee switched on with no keys tells students payment is unavailable. |
| Subscriptions | NOT_STARTED | NOT_STARTED | NOT_STARTED | NOT_STARTED | Not applicable yet — one-time registration only. |
| Settings | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | **Milestone 5, extended in Milestone 14.** The Account settings section of `/profile`: change password (`POST /me/change-password`, requiring the current password so a borrowed session cannot lock the owner out — revokes other devices and keeps this one signed in), sign out everywhere, read-only email/mobile/status/last-sign-in, and — new in Milestone 14 — **notification preferences**. Changing your password now also sends a security email and posts a notice, which is the standard detection signal for a stolen session. Not covered: changing the email address or mobile number (needs a confirm-at-the-new-address flow). **Administrator self-settings now work**: since Milestone 11 the root super admin is a real `Student` document, so it can change its own password from account settings like anybody else — the earlier note that it "has no database record at all" is obsolete. |
| Hall of Fame / Champions | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | The Landing page's "Top Scholars" section, plus the four headline figures above it, are fed by the real `GET /leaderboard` and `GET /public/stats`. The hardcoded champions (invented names, schools and solve times) and the invented counts ("142 registrations", "1280 challenges solved", "8.91s", "450+ schools") are deleted. A fresh deployment truthfully shows zeros and "No one is on the leaderboard yet". **Milestone 10 added the dedicated page** this row used to say was missing — the section now links through to `/leaderboard` and `/hall-of-fame`. |
| Photo upload / storage | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | Mandatory at registration, ≤2 MB, JPEG/PNG/WebP. Carried as a base64 data URL in the JSON body (no `multer`); only that route gets a larger body limit. Declared MIME type is checked against the file's **magic bytes**. Stored in its own `StudentPhoto` collection so no student query carries the binary. Served by `GET /students/:studentId/photo` — own photo, or `students:read` re-checked against the database. **Capacity ceiling: ~250 photos on the Atlas free tier.** Not re-encoded, so EXIF is retained; no replace/remove route. |
| Audit logs | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `AuditLog` collection + `/admin/audit-log` page (filter by action and outcome, paginated). Records role changes, status changes, question generation, administrative sign-ins, **and refused privileged requests** with the exact missing permission — a run of those is what an escalation attempt looks like. No TTL: the trail does not expire. Best-effort writes, so a failed audit write never fails the action it describes. |
| Security (baseline hardening) | N/A | IMPLEMENTED | IMPLEMENTED | IN_PROGRESS | Milestone 1 headers/CORS/validation; Milestone 2 token rotation with theft detection, hashed token storage, single-use email tokens, bcrypt cost 12, per-endpoint rate limits, account lockout, no enumeration; Milestone 3 permission-based authorization with database-fresh role checks, immediate session revocation on demotion/suspension, regex-escaped admin search, and an audit trail that records refusals. **The 2026-08-17 audit closed five findings**: CSRF (an `Origin`/`Referer` check mounted once for the whole API — this row's long-standing "top open gap" is no longer open), `localhost:5173` as a production CORS origin, full legal names on the two public lookups, no security headers on the frontend deployment, and no rate limit on the payment routes or the credential-issuing admin routes. Rate limiting, headers and CORS are still not asserted by tests; the CSRF check **is**, by `tests/security.audit.test.ts`. Still `IN_PROGRESS` rather than done: `npm audit` was not run in the audit session, `trust proxy` is unset so every per-IP limit is effectively one shared bucket on serverless, and there is no 2FA. See [`SECURITY.md`](SECURITY.md). |
| Deployment | IMPLEMENTED | IMPLEMENTED | N/A | NOT_STARTED | Both apps have working Vercel configs; production builds verified locally. **Deploy the backend before the frontend.** Milestone 2 adds two prerequisites: set the `SMTP_*` vars (or students get no verification email) and set `FRONTEND_URL` (or emailed links point at localhost). Deploying also **signs everyone out**, because the cookie names changed. Not verified live. |

## Refer & Earn (Milestone 22, Phases E–G) — IMPLEMENTED end-to-end

| Feature | Frontend | Backend | Database | Testing | Notes |
|---|---|---|---|---|---|
| Referral code per student | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `AMIT` + six characters from a 31-symbol unambiguous alphabet, `unique`/`sparse` on `Student`, generated **lazily** so accounts predating the feature need no migration. Not `studentId`: ten thousand ids is walkable, and a code gets posted in WhatsApp groups. |
| Referral link | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | `<FRONTEND_URL>/register?ref=<code>` — built from configuration, never a hardcoded domain (asserted). |
| Server-side attribution | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `registerSchema` accepts an optional `referralCode`; the handler resolves it and **refuses the registration** if it does not, rolling the account back. Case-insensitive. |
| Abuse prevention | N/A | IMPLEMENTED | IMPLEMENTED | TESTED | A **unique index** on `Referral.referred` enforces one referrer per registration, no duplicate attribution and no changing it later — not a handler check that could race. Self-referral refused. A suspended referrer's code stops resolving. No request may supply an amount. |
| Public code validation | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | `GET /referrals/validate` — public (the user has no account yet), rate limited, and publishing a **masked** name only. |
| Conversion on payment | N/A | IMPLEMENTED | N/A (derived) | TESTED | Hooked into `capturePayment()`, the one place money becomes real — **not** `hasEntryEntitlement()`, which is true when the paywall is off. Conditional write, so a duplicate webhook cannot accrue twice. Stale rows reconcile against `Payment` on read. |
| Reward configuration | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `ReferralSettings`, a pinned document like `PaymentSettings`. **Defaults to off and ₹0 — no reward rule was ever specified by anybody, so none was invented.** Eligibility is deliberately *not* configurable. |
| Reward lifecycle | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `pending_conversion → no_reward \| accrued → approved → paid`, or `rejected`. Every transition a **conditional write**; paying unreachable from `accrued`; payout reference and rejection reason mandatory; amount snapshotted at conversion so re-pricing cannot rewrite it. |
| Student referral summary | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | `GET /me/referrals` — code, link, settings, counts and reward totals, all counted from real rows. Referred students **masked**. |
| Admin referral console | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | `GET /admin/referrals` on `students:read`, with programme-wide totals and `referredHasPaid` **derived** from the payment record. The three money acts on the new `referrals:write`. |
| Referral audit trail | N/A | IMPLEMENTED | IMPLEMENTED | TESTED | `referral.reward.changed` (with the amount and the acting administrator) and `referral.settings.updated` (both sides of the change). Attribution and conversion are recorded by the `Referral` document's own timestamps — there is no authenticated actor at either moment. |
| Referral columns in the directory / export | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | Promised in Phase B and held back until the data existed: `Referral Code`, `Referred By`, `Referred By ID`. |

**Phase F (the student page) added**: `/referrals` with the code, link, WhatsApp/native share and
copy buttons, real counts, and the masked list of people invited — plus `?ref=` capture on the
register page, validated before use and reported either way. **Two defects it caught that the 1253
backend tests could not**: the referral link pointed at `/register`, a route that did not exist (no
catch-all, so every link rendered blank), and a `behavior: 'smooth'` scroll whose failure mode is
doing nothing at all.

**Phase G (the admin console) added**: `/admin/referrals` — four programme-wide counted figures, the
reward settings beside what they have accrued, and per-row actions offering only the transitions the
API will accept (Approve on `accrued`, Mark paid only on `approved`, "Closed" once terminal). Paying
and rejecting each demand a note, because both go on a permanent record. **The console cannot invent
money**: no request it makes carries an amount.

**The open owner decision**: set the reward amount and the terms sentence at **`/admin/referrals`**
when it is decided (the settings form is on that page). Until then the console says plainly that
nothing is being accrued and that introductions are still recorded. Until then the programme tracks every introduction and
reports honestly that no reward is configured — it does not display ₹0 as though it were an offer.

## A.M.I.T branding — the full form (Milestone 22, Phase D) — IMPLEMENTED

| Feature | Frontend | Backend | Database | Testing | Notes |
|---|---|---|---|---|---|
| The official full form | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | **A.M.I.T = Advance Mathematics and Intelligence Test**, owner-supplied 2026-08-28 because it appeared nowhere in the repository. Defined once in `frontend/src/lib/brand.ts`, which is what keeps the visible name and the page metadata from drifting apart. |
| Landing page hero | IMPLEMENTED | N/A | N/A | NOT_STARTED | The **only** visible use. Under the wordmark, set as a formal descriptor: uppercase, wide tracking, muted, so it reads as part of the logotype rather than as copy. **Milestone 23 Phase F** rebuilt the hero around it — the gold hairline rules went with the redesign, the wordmark is a gradient with a real colour underneath it (a `background-clip: text` fill is invisible in forced-colors mode, so the fallback is the brand navy, not transparent), and the headline dropped the year `2027`, which the product could not back anywhere — **the owner supplied it on 2026-08-29 and it is back**, as `AMIT_COMPETITION_YEAR` in `lib/brand.ts`, the one place to change it when the sitting moves. |
| About section | **REMOVED** | N/A | N/A | N/A | A first attempt added one — the letters in separate boxes plus two explanatory paragraphs — and the owner rejected it: no boxes, no `and` hanging off the `I`, no explanation, keep it at the top. **A name is displayed, not glossed.** Do not reintroduce it without asking. |
| Footer | N/A (unchanged) | N/A | N/A | N/A | Carries the four-letter name as it always did. A full-form line was added and then removed with the About section. |
| Navbar | N/A (deliberately unchanged) | N/A | N/A | N/A | Keeps the four-letter wordmark; the expansion rides on the logo's `alt` and the link's `title`, so assistive technology and search still reach it. |
| SEO metadata | IMPLEMENTED | N/A | N/A | NOT_STARTED | The expansion in `<title>`, a `<meta name="description">` (**there was none at all before**) and Open Graph title/description. `index.html` is static and cannot import the constant — the one deliberate duplication, commented in both files. |

**Verified in a browser** (2026-08-28) at 1280px and 375px: exactly one visible occurrence on the
page, and the line tightens its tracking and shortens its rules on a phone rather than wrapping.

**Deliberately unchanged**: the certificate PDF and the invoice issuer default still read
`A.M.I.T Maths Olympiad`. Those are documents people keep, and putting the expansion on one is a
change to a printed record that deserves its own decision.

## Content reset (Milestone 22, owner request) — IMPLEMENTED end-to-end

| Feature | Frontend | Backend | Database | Testing | Notes |
|---|---|---|---|---|---|
| Reset the Question Bank | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | Deletes **every** question, all statuses including published — which deliberately overrides `deleteQuestion()`'s refusal to hard-delete a published question. Blocked while any mock test, daily challenge or official exam is built from the bank. Chapters and practice history survive. |
| Reset all mock tests | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | Deletes every `MockTest` **and** `MockTestAttempt` — an attempt whose paper is gone is a row the student's page cannot render. XP is not touched. |
| Reset all daily challenges | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | Deletes every `DailyChallenge` and `DailyChallengeAttempt`, including the automatically filled days. XP and streaks are not touched. |
| Reset all chapters | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | Deletes every `Topic` (chapters and subtopics). Blocked while any question is filed under them. The `Subject` survives, so a replacement chapter can be created immediately. |
| Irreversible-action warning | IMPLEMENTED | N/A | N/A | NOT_STARTED (no frontend test suite) | One shared `components/ResetPanel.tsx`: a red dashed Danger zone, then a blocking dialog with a red alarm band, **real counts** of what will go, a list of what survives, and a **typed phrase** (different per area). Cancel is the visually heavier button. Escape closes it. Browser-verified. |
| Reset authorization | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | `content:reset`, **super admin only** — an ordinary administrator is refused and nothing is deleted (asserted). The panel is not rendered without the permission, and the server refuses regardless. Rate limited 60/hour. |
| Reset audit trail | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | A `content.reset` entry per reset, carrying the scope and the **per-collection counts** — denormalised, because afterwards there is nothing left to count. Shown in the Audit Log page as "Content area reset". |

**Verified in a browser** (2026-08-28) against the local database: the blocked path on Chapters (208
questions named, no confirmation field offered), the confirmation gating, a real reset of the daily
challenges, the dependency chain unblocking afterwards, the audit entry with its counts, and the
leaderboard still ranking students on XP the reset did not touch.

## Student invoices (Milestone 22, Phase C) — IMPLEMENTED end-to-end

| Feature | Frontend | Backend | Database | Testing | Notes |
|---|---|---|---|---|---|
| Invoice for a paid student | IMPLEMENTED | IMPLEMENTED | N/A (derived) | TESTED | *Your receipts* on `/payment`: one row per captured payment, a full preview, and a PDF. Built from the actual `Payment` and `Student` records — no hardcoded amount, name, date, transaction id or number anywhere. 26 tests. |
| Invoice numbering | N/A | IMPLEMENTED | N/A | TESTED | `AMIT-INV-<capture year>-<last 12 hex of the payment id>`, **derived rather than allocated**. Stable for ever, unique by the `ObjectId`, needs no counter, no write on a GET and no migration for payments that predate the feature. Asserted against a literal so the derivation cannot change silently. |
| Idempotent download | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | There is **no `Invoice` collection**, so downloading creates nothing. A test downloads four times and asserts one number and an unchanged document count. |
| Invoice authorization | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | Ownership is part of the query, so another student's id is a **404, not a 403** — and cannot be used to discover which ids exist. Staff reissue is a separate route on `students:read`. No public or guessable invoice URL exists. |
| Invoice PDF | N/A | IMPLEMENTED | N/A | TESTED | `pdf-lib` (already the certificate's library — no new dependency). A4: issuer, billed-to, dates, item table, total, **total in words** in Indian grouping, payment panel, footer. Sanitised so a name in any Indian script cannot 500 the download. |
| Tax / GST on the invoice | N/A | IMPLEMENTED | N/A | TESTED | **Only what is configured.** With no `INVOICE_GSTIN` the document is titled `INVOICE` and says nothing about tax — no rate, no "inclusive of all taxes", no placeholder address. With one it becomes a `TAX INVOICE`. Six new optional env vars. |

**Verified in a browser** (2026-08-28): the receipts list, the preview modal with real figures, the
student PDF download (200, `no-store`, correct filename), the staff download from the payments console,
a stranger's id returning 404, and the generated PDF's own text extracted and checked line by line.

**The entry fee is Rs.199** as of 2026-08-28 (owner decision, up from Rs.100). It is a
`PaymentSettings` value changed at `/admin/payments`; the code default was updated to match and applies
only where no settings document exists. An invoice for an earlier payment still reads Rs.100, because
`Payment.amount` is a snapshot — there is a test.

## Admin student directory and Excel export (Milestone 22, Phase B) — IMPLEMENTED end-to-end

| Feature | Frontend | Backend | Database | Testing | Notes |
|---|---|---|---|---|---|
| Student directory (all registrations) | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | `/admin/users`, renamed **All Students**. `GET /admin/students` widened from a `find()` to the aggregation in `services/studentDirectoryService.ts`. **Every registered account appears whatever its payment state** — the payment filter is absent by default and must stay absent. Search now includes `schoolName`. 28 tests. **Redesigned in Milestone 23 Phase E**: a 13-column table inside a `TableScroll` above 768px and **one card per student below it**, with no column dropped — the fields are records, not a comparison. Plus pagination (20 a page), a skeleton table while loading, an `EmptyState` that says *hidden by a filter, not missing*, `Badge`s for status and payment state, and one `AccountActions` component rendered by both layouts so the row menu cannot drift between them. |
| Payment state per student | IMPLEMENTED | IMPLEMENTED | N/A (derived) | TESTED | `paid` / `pending` / `failed` / `refunded` / `not_started`, rolled up in the aggregation from that student's own `Payment` rows on every read. **No stored field**, for the reason there is no `hasPaid` flag. `paid` outranks a prior failure, and the payment shown is the capture. No `cancelled` state, because the platform has no such payment status. |
| Directory filters and sorting | IMPLEMENTED | IMPLEMENTED | IMPLEMENTED | TESTED | Class, payment status, inclusive registration-date range, account status, role, verified, free-text search; eight sort orders from an allow-list mapped to fields. Server-side pagination throughout — the browser never holds more than one page. Two new `Student` indexes back the default order and the class filter. |
| Excel export | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | `GET /admin/students/export` → `.xlsx` via `exceljs` (already a dependency). 24 columns plus an `About this export` sheet. Respects the administrator's filters, or `scope=all` for the whole roll. Dates as dates, money as a number in rupees. Over 20,000 rows is **refused with a message**, never truncated. Rate limited 30/hour. |
| Export data safety | IMPLEMENTED | IMPLEMENTED | N/A | TESTED | The renderer has **no database access** and can only write what the shared view holds, so no password, hash, token, key or payment signature is reachable. Tested against both the column headings and every cell value — and against the listing's whole JSON body, because `select: false` does not apply to an aggregation. |

**Verified in a browser** (2026-08-28) against a local database seeded with a student in each of the five
payment states: all nine accounts listed, each state derived correctly, the payment filter narrowing the
list and updating both the count and the button label, and three export scopes returning real `.xlsx`
files with the server-chosen filename. The admin dashboard, which reads the same endpoint, is unaffected.

**Not in Phase B, deliberately**: referral columns. `Referral code` and `Referred by` do not exist in the
product yet, and a column the application cannot fill is not a column — they are added to the directory
and the export in Phase E, when the data behind them exists.

## Bulk question import (Milestone 21) — IMPLEMENTED end-to-end and usable by an administrator

| Capability | Status | Notes |
| --- | --- | --- |
| Import pipeline (parse → place → screen → preview → approve) | **IMPLEMENTED, TESTED** | `services/questionImportService.ts`. 62 tests. |
| Upload validation (magic bytes, MIME, extension, size, count, filename) | **IMPLEMENTED, TESTED** | `validation/uploadSchemas.ts`. Nothing written to disk. |
| Duplicate detection (in-file, cross-file, against the bank) | **IMPLEMENTED, TESTED** | Reuses the generator's `similarity()` and the one shared screener. |
| Question validation for imports | **IMPLEMENTED, TESTED** | The *same* `createQuestionSchema` a hand-authored question passes. No second validator. |
| Class validation for imports | **IMPLEMENTED, TESTED** | Centralised in `lib/classLevels.ts`, already the single source. Still `Class 5`–`Class 11` + three Class-12 streams — **the 3–12 range is Phase J**. |
| Approve / reject, with provenance and audit | **IMPLEMENTED, TESTED** | Approval is the only writer; questions are created as `draft`. |
| Admin authorization on import routes | **IMPLEMENTED, TESTED** | `questions:write`, asserted 403 for a student on both URL prefixes. |
| Rate limiting + server-side bulk limits | **IMPLEMENTED**, not asserted | `importLimiter` (limiters are disabled under test, as everywhere). |
| **Excel parsing** | **IMPLEMENTED, TESTED** | Phase C. `services/excelImportParser.ts`, 57 tests against real `.xlsx` files. All five question types; any column order; loose headings; every sheet; per-row class/chapter/difficulty. |
| **Excel template download** | **IMPLEMENTED, TESTED** | Phase C. `GET /admin/questions/import/excel/template`, generated per request so its dropdowns cannot go stale. A test asserts its own examples import cleanly. |
| **DOCX parsing** | **IMPLEMENTED, TESTED** | Phase D. `services/docxImportParser.ts`, 38 tests against real `.docx` files. Six numbering styles, four option styles, per-question metadata, wrapped paragraphs rejoined. Deterministic — **no AI**, deliberately. Warns about Word auto-numbering and Word equation objects, the two failures that look like success. |
| **Image extraction via Gemini** | **IMPLEMENTED, TESTED** | Phase E. `services/imageImportParser.ts`, 39 tests, none touching the network. The model **transcribes what is printed**; `lib/importAnswerText.ts` derives the answer key, so no new answer reading was needed. One model call per image. Needs `GEMINI_API_KEY` — without it this format alone reports unavailable and Excel and Word carry on. A question with no printed answer is refused, never given one. |
| **Import review UI** | **IMPLEMENTED, verified in a browser** | Phase F. `/admin/questions/import` with a **Bulk Import** sidebar entry. One page, three format tabs, one review screen. Counts strip, per-file table, failures/rejections/duplicates named with their row numbers, CSV problem list, per-card editing including class/chapter/difficulty/type, dry-run "Check before saving", approve as drafts or publish. **No frontend tests exist** — verified by driving it against the local dev database, which found two client-side bugs. |
| **Practice assignment** | **IMPLEMENTED, TESTED, verified in a browser** | Phase G. Owner decided: **publishing is the assignment**, no `PracticeSet` collection — Practice samples what is published for the student own class, so there is nothing to assign to. `PATCH /admin/questions/bulk-status` publishes a selection (looping `changeQuestionStatus()` so `assertPublishable()` still runs), and `GET /admin/questions/practice-availability` previews what a class can practise using the student picker own function. 11 tests. |
| **Mock Test / Daily Challenge assignment** | **IMPLEMENTED, verified in a browser** | Phases H and I. From the Question Bank, **Create mock test** (one shared class, ≤100) and **Schedule daily challenge** (exactly one, published) hand the selection to the existing authors via a query string. **No backend change** — both APIs already took explicit question ids. `pages/Admin/questionHandoff.ts` owns the rules and returns either a URL or the reason the action is unavailable, so the buttons explain themselves. The browser pass found and fixed a pre-existing out-of-order-response race in both question pickers. |
| **Chapter optional + auto-detected** | **IMPLEMENTED, TESTED, verified in a browser** | `lib/chapterDetection.ts`, pure and deterministic — **no model**. Optional on the import form and suggested in the manual editor from one shared function. Three outcomes: matched (with a note naming the matched words), ambiguous (names the candidates, never picks), none (reports the row by number). 24 tests. |
| **Chapter-wise / whole-syllabus paper** | **IMPLEMENTED, TESTED, verified in a browser** | `GET /admin/questions/paper-suggestion` plus a Fill control in the mock-test author. The whole-syllabus option **spreads** across every chapter with published questions rather than taking the most recent, and is scoped to the implicit subject — it was returning Physics chapters from legacy data before that. |
| **Classes 3–12** | **IMPLEMENTED, TESTED, verified in a browser** | Phase J. `CLASS_LEVELS` is a flat ten; the three Class-12 streams are retired into one, so **a Commerce and a Science student now sit the same papers**. `scripts/migrate-class-levels.ts` is report-only until `--write` and refuses while daily-challenge unique-index collisions are unresolved. **Not yet run against production — that is the owner's to run.** |
| **Removing user-facing Subject** | **IMPLEMENTED, verified in a browser** | Phase J. Every picker, filter and column gone across nine screens; taxonomy page renamed **Chapters**. The `Subject` model, `/subjects` routes and `Question.subject` stay — `Topic` is scoped by subject. `subject` is optional on the write schemas and derived from the chapter, with the cross-check kept for callers that still send one. Physics seed deleted. |

**What an administrator can actually do at the end of Phase F: the whole thing, from the admin
panel.** Open **Bulk Import**, choose Excel / Word / Photographs, download the Excel template,
set what the questions should be filed under, upload, read exactly what was and was not
extracted, correct anything, check it, and approve as drafts or publish. Imported questions are
ordinary questions from that point on — a published one appears in Practice for its class.

Verified by driving it in a browser against the local dev database, because **the frontend has no
test suite**: a five-row workbook (three good rows, one with no answer, one claiming `Class 13`)
reported 5 examined / 3 usable / 1 invalid / 1 unreadable, named both problems with their row
numbers, and saved three `draft` rows stamped `excel_import`. That pass found two client-side
bugs the 1,086 backend tests could not — see `TROUBLESHOOTING.md`.

Three limits are **permanent rather than pending**, and belong here rather than in a release note:

- **Equations built with Word's equation editor cannot be read** — `mammoth` drops them, and they
  must be retyped as `$…$`. The importer warns when it sees the markup.
- **Nothing a diagram carries can be imported, in any format**, because `Question` has no image
  field. That remains on the "not started at all" list. An image import flags such a question
  rather than importing it half-complete.
- **An image transcription is not verified.** OCR of mathematical notation fails quietly, so every
  image import carries a standing warning and the human review step is the only control. A
  question with no printed answer is refused rather than given one.

---

## Infrastructure / foundation (Milestone 1)

These are backend capabilities, not user-facing features. `TESTED` here means covered by `backend/tests/`.

| Capability | Status | Testing | Notes |
|---|---|---|---|
| Modular backend structure | IMPLEMENTED | N/A | `config/`, `db/`, `lib/`, `middleware/`, `models/`, `routes/v1/`, `validation/`; `app.ts` builds the app, `server.ts` bootstraps the process. |
| Environment validation | IMPLEMENTED | NOT_STARTED | zod schema in `config/env.ts`; loads `.env` via dotenv (skipped under `NODE_ENV=test` to keep tests hermetic). Production throws if `JWT_SECRET` is unset. |
| Application configuration | IMPLEMENTED | NOT_STARTED | Typed `config` object in `config/index.ts`; nothing else reads `process.env` directly. |
| MongoDB connection module | IMPLEMENTED | NOT_STARTED | `db/connection.ts` — cached/de-duplicated connect, explicit 8s server-selection timeout, state helpers. Live Atlas connectivity **unverified from the dev sandbox**. |
| Per-request DB gate | IMPLEMENTED | IN_PROGRESS | `middleware/ensureDb.ts` — required because the Vercel serverless entry imports the app directly and never runs `server.ts`. Returns 503 when the DB is unreachable. Verified manually (clean 503 in 93ms); no unit test. |
| API versioning | IMPLEMENTED | TESTED | `/api/v1/*` canonical, `/api/*` compatibility alias; a test asserts both return the same status. Frontend migrated to `/api/v1`. |
| Global error handling | IMPLEMENTED | TESTED | `middleware/errorHandler.ts` + 404 handler; consistent `{success:false, error}` envelope, internals hidden in production. |
| Consistent API response structure | IMPLEMENTED | TESTED | `lib/apiResponse.ts` (`sendSuccess`/`sendError`) + `lib/ApiError.ts`. |
| Request validation architecture | IMPLEMENTED | TESTED | `middleware/validate.ts` with zod schemas per route. Uses `Object.defineProperty` because `req.query` is getter-only in Express 5. |
| Structured logging | IMPLEMENTED | NOT_STARTED | pino; pretty in dev, JSON in production, silent in tests. |
| Request logging | IMPLEMENTED | NOT_STARTED | `pino-http`, log level derived from response status. |
| Security headers | IMPLEMENTED | NOT_STARTED (deliberate) | `helmet` with defaults, `x-powered-by` disabled. |
| CORS configuration | IMPLEMENTED | TESTED | Explicit origin allow-list; never falls back to reflecting any origin. **Since 2026-08-17, `http://localhost:5173` is admitted only outside production** — it used to be unconditional, so the deployed API accepted credentialed cross-origin requests from any page a visitor was serving on that port. A test asserts the derivation rather than a copy of the rule. |
| **CSRF protection** | IMPLEMENTED | TESTED | **New, 2026-08-17.** `middleware/csrf.ts`, mounted once in `app.ts` so it covers `/api/v1` and the `/api` alias together: every `POST`/`PUT`/`PATCH`/`DELETE` must carry an `Origin` (or, failing that, a `Referer`) whose host is in the CORS allow-list or is the request's own. Reads are not policed; a request with neither header is allowed, because a browser always sends one on an unsafe method and CSRF is by definition a browser attack. No token is issued — see the ADR for why one was considered and deliberately not stacked on top. |
| Rate limiting | IMPLEMENTED | NOT_STARTED (deliberate) | `express-rate-limit`: general limiter on `/api`, plus per-route limiters for login, registration, email actions, token submission, refresh, self-service account changes, practice, mock tests, the daily challenge and — since 2026-08-17 — the **payment** routes, the credential-issuing **admin** routes and the **public lookups**. `/health` + `/ready` mounted before it so probes are never throttled. Disabled under `NODE_ENV=test`, which is why this stays untested. **Caveat: `trust proxy` is unset**, so on serverless every per-IP limit is effectively one shared bucket — deliberately not changed, because the naive fix makes the limits spoofable. See `SECURITY.md`. |
| Frontend delivery headers | IMPLEMENTED | NOT_STARTED (static config) | **New, 2026-08-17.** `frontend/vercel.json` sends `X-Frame-Options: DENY`, CSP `frame-ancestors 'none'`, `X-Content-Type-Options: nosniff`, `Referrer-Policy` and a narrow `Permissions-Policy`. It previously sent **none**, so the signed-in SPA could be framed by any site. Only `frame-ancestors` in the CSP on purpose: a `default-src` policy would have to enumerate Google Fonts, unpkg and Razorpay's checkout, and one written without a browser to check it against is a broken page rather than a safer one. |
| Health endpoint | IMPLEMENTED | TESTED | `GET /health` — liveness, no DB dependency. Verified live: 200. |
| Readiness endpoint | IMPLEMENTED | TESTED | `GET /ready` — 200 when Mongo is connected, 503 otherwise. Verified live: correctly reported 503 with the DB down. |
| Graceful shutdown | IMPLEMENTED | NOT_STARTED | SIGTERM/SIGINT close the HTTP server, then Mongo, with a 10s forced-exit fallback. Local bootstrap only (not the serverless path). |
| Development scripts | IMPLEMENTED | N/A | `dev` (tsx watch), `start`. |
| Production build scripts | IMPLEMENTED | N/A | Deliberately named **`compile`** (`tsc -p tsconfig.json`), emitting only `src`/`api` to `dist/`. It must **never** be renamed to `build`: Vercel's zero-config sees a `build` script, runs it, and then fails the deployment with `No Output Directory named "public" found`, because the backend is a serverless function rather than a static site. This exact regression broke production once — see `TROUBLESHOOTING.md`. |
| Type checking | IMPLEMENTED | N/A | `typecheck` (`tsc -p tsconfig.test.json`) covers src + api + tests. Passes. |
| Linting (backend) | IMPLEMENTED | N/A | `eslint` + `typescript-eslint` flat config. Passes clean. |
| Centralized authorization | IMPLEMENTED | TESTED | `lib/permissions.ts` holds the only role → permission table — **22 permissions** (`exam:write` and `certificates:write` added in Milestone 13; `gallery:write` and `notifications:write` in Milestone 12; `users:password:reset` and `users:sessions:revoke` in Milestone 11; unchanged by Milestone 10, whose two new routes are both public; `rewards:write` added in Milestone 9; `challenges:write` in Milestone 8; `mocktests:write` in Milestone 7; `questions:delete` and `taxonomy:write` in Milestone 4); `middleware/auth.ts` exposes `requirePermission` (the preferred gate), `requireAuth` (identity-only gates), `callerCan` and `callerCanFresh`. No handler compares a role to a literal. |
| Database-fresh role checks | IMPLEMENTED | TESTED | Privileged requests re-read `role`/`status`/`tokenVersion` from MongoDB, so the token's role claim cannot outlive a demotion. Student-level requests remain stateless. **No account is exempt any more:** Milestone 11 gave the root super admin a real document and removed the exemption in `resolveCurrentRole()`, so every privileged request — including the most privileged one — is checked against the database. |
| Audit trail | IMPLEMENTED | TESTED | `AuditLog` model + `lib/audit.ts` recorder + `GET /admin/audit-logs`. Records successes and refusals; no TTL; best-effort writes that never fail the underlying action. |
| Permission-aware frontend guards | IMPLEMENTED | NOT_STARTED (no frontend test suite) | `RequirePermission` route guard, `Unauthorized` state component, `can()` from `AuthContext`, permission-filtered navigation in `Navbar` and `AdminShell`. The navbar's **Admin** link shows for a guest (so the sign-in form is reachable without typing the URL) and for anyone holding `students:read`, but is hidden from a signed-in plain student, who would only reach `Unauthorized` by following it. Verified manually in a browser, including client-side tampering attempts. |
| Backend testing foundation | IMPLEMENTED | TESTED | vitest + supertest, **796 passing tests** across 24 files. 21 are the Milestone 18 question-generator suite (mostly feeding the pipeline what a real model could plausibly produce and asserting it is refused — a forbidden LaTeX command, markup in question text, two correct options on a single-choice question, a model trying to pick its own taxonomy); 36 are the Milestone 16 recommendation suite (weighted toward asserting a finding is *not* produced — a 40% topic that must not be called weak, a perfect five that is not a strength, an engine that cannot describe itself); 32 are the Milestone 15 performance-analytics suite (every case seeds attempts with declared outcomes, so it asserts an exact figure rather than that a number exists); 46 are the Milestone 14 notification suite (most of it failure handling, via a test-only hook that makes the mail transport fail on demand); 50 are the Milestone 13 official-exam and certificate suite; 43 the Milestone 12 admin-platform suite (gallery, notifications, analytics, standings — with every new route's authorization asserted for a guest *and* a student on **both** the `/api/v1` and `/api` prefixes); 27 the Milestone 11 account-administration suite; 49 the Milestone 10 leaderboard/Hall-of-Fame suite; 31 the Milestone 9 gamification suite; 37 the Milestone 8 daily-challenge suite; 54 the Milestone 7 mock-test suite; 42 the Milestone 6 Practice Zone suite; 71 the Milestone 5 dashboard/progress/analytics/results suite and 27 the Milestone 5 profile suite; 77 the Milestone 4 question-bank suite; 62 the Milestone 3 RBAC/privilege-escalation suite; 40 the registration-details suite; 32 Milestone 2 auth integration tests; the rest need no database. Files run **sequentially** (`fileParallelism: false`) because ten suites each start a real `mongod`. |
| Student activity log | IMPLEMENTED | TESTED | `StudentActivity` model + `services/activityService.ts`. The single source of truth for XP, levels, streaks and achievements — nothing is stored as a counter. Once-per-day and once-per-account are enforced by a **partial unique index**, not by a check, so two concurrent requests cannot both earn the same event; `recordActivity()` treats a duplicate-key error as "already counted". Writes are best-effort and never throw, so a failed log write cannot fail the registration or password change it describes. No TTL. |
| Progress derivation | IMPLEMENTED | TESTED | `services/progressService.ts` — XP as a `$sum`, streaks from `distinct('occurredOn')`, the leaderboard and the caller's rank as aggregations, and `getRecentExamPerformance()` reading the (still empty) `ExamAttempt` collection. `lib/xp.ts` holds the award table and the level function; `lib/achievements.ts` the achievement catalogue; `lib/competitionDay.ts` the IST day boundary. Scale note in the code: the leaderboard groups the whole collection, which suits a first cohort of a few hundred and is isolated in one function. |
| Session cookie helpers | IMPLEMENTED | TESTED | `lib/session.ts` — extracted from `auth.routes.ts` in Milestone 5 because the password change also has to issue a session. One definition of what a session cookie is and what claims the access token carries, rather than two. Covered indirectly by the whole auth suite, which still passes unchanged. |
| Activity backfill script | IMPLEMENTED | NOT_STARTED | `backend/scripts/backfill-activity.ts` gives pre-Milestone-5 accounts the enrolment rows they already earned, dated from their real `registeredAt`. Report-only by default; `--write` applies; idempotent via the unique index. Deliberately does **not** invent `daily_visit` rows, so no streak is fabricated. |
| Access / refresh token service | IMPLEMENTED | TESTED | `lib/tokens.ts` — 15-min access JWT with a `tv` revocation claim, plus opaque 30-day refresh tokens stored SHA-256-hashed and rotated on use, with family-wide revocation on reuse. |
| Session / token revocation | IMPLEMENTED | TESTED | Per-device (`logout`), everywhere (`logout-all`), and automatic on password reset, **role change and suspension** (Milestone 3). For privileged requests the 15-minute access-token window is no longer a gap: the role is re-read from the database, so a revoked admin is refused at once. |
| Password hashing | IMPLEMENTED | TESTED | `lib/password.ts`, bcrypt cost 12 (4 under test). `passwordHash` is `select: false` so it cannot leak. |
| Email delivery | IMPLEMENTED | TESTED | **Rebuilt in Milestone 14.** `lib/email.ts` holds the transport (`nodemailer` over SMTP, provider-agnostic, a log transport when unset, an in-memory one under test) and `deliverEmail()` now **throws** on failure instead of swallowing it. `services/emailOutbox.ts` is THE queue and the only caller: persist first, return, then deliver off the request path with backoff, a terminal give-up and a conditional-write claim. **SMTP is configured and delivery to a real inbox is confirmed** (owner, 2026-08-12). Promoted from `IN_PROGRESS` because the *failure* behaviour is now genuinely asserted — a test-only `failNextDeliveries()` hook makes the transport fail on demand, so retry, recovery, terminal failure and no-double-send are all covered without depending on a real provider. |
| Account status handling | IMPLEMENTED | TESTED | `active`/`suspended`/`deactivated`, enforced at login, on refresh, on every `/auth/me`, and on every privileged request. **Milestone 3 adds the admin UI** (`/admin/users`) — a direct database edit is no longer the only way to set it. Suspending revokes sessions immediately; reactivating clears the lockout counters. |
| Account lockout | IMPLEMENTED | TESTED | 5 failed logins → 15-minute lock, returning 423 even for the correct password. |
| Auth state restoration | IMPLEMENTED | NOT_STARTED | `AuthContext` tries `/auth/me`, then one `/auth/refresh`, before concluding "guest". Verified by a real browser reload, not by automated test. |
| Transparent token refresh (frontend) | IMPLEMENTED | NOT_STARTED | `api/client.ts` retries once after a 401, de-duplicating concurrent refreshes through a shared promise so rotation cannot trip theft detection. Verified manually. |

## Bulk import: naming a chapter the bank does not have — RESOLVABLE IN ONE ACTION

Chapters are **not class-scoped** (`Topic` has no `classLevel`), so there is one flat chapter list per
subject and each new class needs its chapters adding once. A bank seeded for Class 12 therefore
refuses every row of a Class 9 paper.

| Behaviour | State |
| --- | --- |
| A row naming an unresolvable chapter | Refused, with its row number — unchanged |
| An importer creating a `Topic` itself | Never, by design |
| The distinct missing names | Returned as `ImportPreview.unknownChapters` |
| Fixing them | One button on the review screen, then the file is re-read |
| Creating them | `POST /admin/chapters/bulk` (`taxonomy:write`), top-level only, 1–60 names |
| Partial success | 200 with `created` / `existing` / `failed` per name |
| Spelling protection | The examiner reads the verbatim list before the button |

## Mathematics-only scope (Milestone 21, Phases J and L) — ENFORCED, not just hidden

Phase J removed every user-facing Subject control. Phase L found that removing the *control* had
left the *queries* unfiltered, and closed that.

| Surface | State |
| --- | --- |
| Practice availability (`/practice/options`) | Scoped to the implicit subject |
| Practice session draw (`POST /practice/sessions`) | Scoped when the caller names no subject |
| Daily challenge automatic pick | Scoped |
| Dashboard "Available practice" tile | Scoped |
| Chapter pickers (6 screens) | Scoped, via one shared frontend resolver |
| Mock-test paper builder — question picker | Scoped (`subject=<implicit>`) |
| Daily-challenge scheduler — question picker | Scoped (`subject=<implicit>`) |
| **Question Bank (`/admin/questions`)** | **Deliberately unscoped** — legacy data must stay manageable |
| Analytics strengths/weaknesses | No subject-scope findings |
| Any subject name rendered to a user | None |

What is **not** done, and is deliberate: there is no second-subject product. `Subject`, `/subjects`
and `Question.subject` all still exist and the architecture is still open to one, but nothing seeds
one, nothing offers a choice, and adding one back means revisiting the two Phase L ADRs rather than
flipping a flag.

Retiring legacy subject data is `npm run retire:subjects --prefix backend` (report-only until
`--write`). **Not yet run against production.**

## Summary counts

- **Phase H (the regression pass)**: all thirteen areas named in the brief driven in a browser against a real backend — registration, login, both dashboards, payment, the question bank, practice (started, answered, submitted, graded), mock tests (authored → published → visible), the daily challenge, AI generation, bulk import, analytics and recommendations — plus the public surfaces. **No regression found**, no console error on any page, and the answer-key rule re-verified in both directions.
- **New in Milestone 22 (Phase G)**: the admin referral console at `/admin/referrals` — counted programme-wide totals, the reward settings, filters and search, and the approve / mark-paid / reject lifecycle with a mandatory note on the two that go on the record. **No backend change, no new dependency.**
- **New in Milestone 22 (Phase F)**: the student `/referrals` page — code, link, share and copy, real counts, a masked list, and an honest empty-reward state that shows **no** earnings tiles rather than ₹0.00. Plus `?ref=` capture on the register page and the `/register` route the referral link had been pointing at all along. **No backend change, no new dependency.**
- **New in Milestone 22 (Phase E)**: the Refer & Earn **backend** — a code per student, server-validated attribution decided by a unique index, conversion driven by captured payments, a full reward lifecycle on conditional writes, an admin console and an audit trail. **The reward itself defaults to off and ₹0, because no amount or eligibility rule has ever been specified** — that is the open owner decision, not an oversight. Two new models (`Referral`, `ReferralSettings`), one new permission (`referrals:write`), two new audit actions, one new service, one new route module. **No new dependency, and no UI yet** (Phases F and G).
- **New in Milestone 22 (Phase D)**: the officially approved full form of A.M.I.T — **Advance Mathematics and Intelligence Test** — shown once under the wordmark on the landing page, plus the SEO metadata, defined once in `frontend/src/lib/brand.ts`. **No new dependency, model, permission, route or page.**
- **New in Milestone 22 (the content reset, owner request)**: a per-area reset for the Question Bank, Mock Tests, Daily Challenges and Chapters, behind a super-admin-only permission, a typed confirmation phrase and a preview that counts what will go. It **refuses rather than cascades**, and never takes back XP. **One new permission** (21 → 22, the first since Milestone 13), one new audit action, one new service, one new route module, one shared component. **No new model, no new dependency.**
- **New in Milestone 22 (Phase C)**: student invoices — a preview and a PDF for every captured payment, a **derived** invoice number that is stable and needs no counter, staff reissue, and an issuer block that prints nothing it has not been told. **No new dependency** (`pdf-lib` was already there), **no new model** (an invoice is a rendering of a `Payment`), **no new permission** (staff reissue reuses `students:read`), **no new page**. Six new optional environment variables.
- **New in Milestone 22 (Phase B)**: the admin student directory (every registration, whatever its payment state), payment state **derived** per student, class / payment / date filters with sorting and server-side pagination, and an `.xlsx` export that respects those filters. **No new dependency** (`exceljs` was already there), **no new model** (the rollup is derived), **no new permission** (it reuses `students:read`), **no new page** (it widened `/admin/users`). Two indexes added to `Student`.
- **New in Milestone 21**: bulk question import from Excel, Word and photographs (two-phase, nothing written until approval); deterministic chapter detection; chapter-wise and whole-syllabus paper building; classes narrowed to **3–12** with the three Class-12 streams collapsed; and the **Mathematics-only scope**, which Phase L turned from a hidden control into an enforced query (see the table above). Two new dependencies (`exceljs`, `mammoth`), one new model (`ImportBatch`), two new scripts (`migrate:classes`, `retire:subjects`), **no new permission** (it reuses `questions:write`).
- **New in Milestone 20**: the AI generator on the official `@google/genai` SDK with structured output; subtopic support; a dry-run validation endpoint; a rejection-recording endpoint; per-question selection and approval; advisory quality warnings (`lib/questionQuality.ts`, pure); `Question.provenance` and a `source` filter on the admin listing; `GenerationLog.rejectedByReviewer`; a `generationLimiter`; four new optional env vars; and `npm run verify:gemini`. **One new dependency** (`@google/genai`, free, official, owner-instructed), **no new model**, **no new permission** (it reuses `questions:write`), **no new page**.
- **New in Milestone 16**: performance recommendations (five kinds, on `/analytics`), and the swappable engine seam behind them. No new model, no new permission, no new dependency, no new page — one new endpoint and one new environment variable with a working default.
- IMPLEMENTED end-to-end (user-facing): Registration (the full entrant record), Email verification, Login, Logout, Forgot password, Reset password, Admin auth (root + promoted), RBAC, Student management, Admin dashboard, Audit logs, Photo upload/storage, Question bank, Deployment config, Student profile, Account settings, Student dashboard, XP, Levels, Streaks, Achievements, Badges, Journey map, the reward engine, Practice Zone, Mock tests, Daily challenge, Leaderboards, Hall of Fame, Gallery, in-app Notifications, platform Analytics, the official exam, published Results, Certificates and public certificate verification — and, **new in Milestone 14, system notifications, email notifications on a non-blocking outbox, notification preferences and an email delivery console**.
- TESTED against a real database: Registration **and every persisted registration field**, photo storage and its authorization, Email verification, Login, Logout/logout-all, Forgot password, Reset password, RBAC and privilege escalation, token revocation, account status, lockout, role assignment, audit logging, admin listing/filtering/search, profile reads and edits (including that a student cannot change their own role or status), photo replacement, password change and its session eviction, XP accrual and its idempotency, level thresholds, streak rules, achievement evaluation, leaderboard ranking and name masking, public counts, **and Milestone 13's exam and certificate properties: an answer refused after the deadline and not stored, a second attempt refused by the unique index, a score invisible before release, a mock test earning no certificate, a forged verification code leaking nothing, a revoked certificate refusing further downloads, and authorization asserted on both URL prefixes**.
- Mostly mock/frontend-only despite a real-looking page: **nothing**. This list is finally empty. The Admin dashboard left it in Milestone 3, the Question bank in Milestone 4, the Student dashboard / Leaderboards / Daily challenge in Milestone 5, and **Exam, Results and Certificates in Milestone 13**. Analytics is a partial case rather than a mock one: its XP half is real and its accuracy half is honestly empty because nothing writes `StudentAnalytics`.
- Not started at all: Subscriptions, two-factor auth, a frontend test suite, question images/diagrams, changing your own email address or mobile number, hard-deleting a *verified* account, writing `StudentAnalytics`, an official-exam Hall of Fame board, a *past competition's* archived hall of fame, and a **scheduled** heartbeat for the email queue (it drains opportunistically and on later requests; on an idle site a queued message waits, mitigated by an explicit staff "send now" — see `PROJECT_STATE.md` known bug #41). **Payments** left this list in Milestone 19, along with the fake QR step it replaced; **System notifications, email notifications, notification preferences and delivery visibility** in Milestone 14; the **official exam, Results, Certificates and public verification** in Milestone 13; **in-app Notifications and Gallery** in Milestone 12; a dedicated Hall of Fame page in Milestone 10.

  **The largest unverified surface is now the frontend**, which still has no test at all across 49 routes — including, as of Milestone 19, the checkout page. That, plus driving one real Razorpay payment through a browser, is what stands between here and the owner's stated next phase of full testing.
- Deliberately dropped: SMS/mobile OTP verification. The fake client-side OTP step was deleted and replaced by real email verification; no SMS provider is integrated (it would also breach the ₹0 constraint).
- Deliberately out of scope in Milestone 3: hard-deleting an account (deactivation is the reversible equivalent), a second super admin via the API, and a frontend test suite (which needs a `DECISIONS.md` entry first).
- Nothing anywhere is marked `DEPLOYED` — no milestone has been deployed or verified in production.
